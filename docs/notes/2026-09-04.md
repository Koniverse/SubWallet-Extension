# Notes — 2026-09-04

QC tracker for [SubWallet-Mobile #2057](https://github.com/Koniverse/SubWallet-Mobile/issues/2057)
(*Update web-runner 1.3.86*). **Updated 2026-09-09** — 19 of 23 done.

**Tick a box here when the tracker box is ticked.** Fix status is dev-side and lives here; the
tester's own verification is [US-42.24.20](../sprints/stories/US-42.24.20-qc-web-runner-verify-bugs.md)
and is not mirrored here.

**The tracker is two comments, and the second one is still being written.** Items 1–20 are in the
[09-03 comment](https://github.com/Koniverse/SubWallet-Mobile/issues/2057#issuecomment-5520087148);
items 21, 22 and 23 are in a [second
comment](https://github.com/Koniverse/SubWallet-Mobile/issues/2057#issuecomment-5584157750) opened
on 09-08 and **edited again on 09-09 at 04:46 UTC**, four minutes before this read. That comment now
ends with an **empty `- [ ]`** — a checkbox with no text after it. Item 24 is being typed. Read both
comments, and take any count off the second one as a snapshot of an edit in progress.

## Bugs

| ✔ | Item | Bug | Sev |
| --- | --- | --- | --- |
| [x] | 1. NFT ERC-721 not auto-detected on import/attach | `BUG-42.24.1-01` | P1 |
| [x] | 2. Network toggle / disconnected badge | `BUG-42.24.2-01` + `-02` | P1 ×2 |
| [x] | 3. Price chart missing on token detail | `BUG-42.24.2-03` | P2 |
| [x] | 4. "Migrate to Unified Account" shown twice on import (iOS) | `BUG-42.24.19-01` | P2 |
| [x] | 5. Create-password screen freezes on fresh install | `BUG-42.24.19-02` | P1 |
| [ ] | 6. Done button layout on the Amount screen (iOS 26.6.1) | `BUG-42.24.19-03` | P2 |
| [x] | 7. NFT transfer on EVM refused — names the account, not the network | `BUG-42.24.19-04` | P1 |
| [x] | 8. Substrate NFTs not displayed | `BUG-42.24.19-05` | P1 |
| [ ] | 9. Infinite loading after reopening the app, intermittent | `BUG-42.24.19-06` | **P1** |
| [x] | 10. Crash tapping (+) when importing a custom network (Android) | `BUG-42.24.19-07` | P2 |
| [x] | 11. Wrong content on the (i) tooltip of the locked-balance Others row | `BUG-42.24.19-08` | P2 |
| [x] | 12. Account name indented on Create derived account (Android) | `BUG-42.24.19-09` | P2 |
| [x] | 13. Remove the Available balance field on Add proxy | `BUG-42.24.5-01` | P2 |
| [x] | 14. Call data cannot be scrolled on the Add proxy confirmation sheet | `BUG-42.24.5-02` | P2 |
| [x] | 15. Proxy type option — colour, missing dropdown, Governance offered | `BUG-42.24.5-03` + `-04` | P2 ×2 |
| [x] | 16. Double colon on the Available balance label, Remove proxy | `BUG-42.24.5-05` | P3 |
| [x] | 17. Remove proxy confirmation title truncated with space to spare | `BUG-42.24.5-06` | P3 |
| [x] | 18. Android back button does nothing on the History screen **and all confirm screens** | `BUG-42.24.19-10` | P2 |
| [x] | 19. Spacing between sections on the Unstake screen | `BUG-42.24.19-11` | P3 |
| [x] | 20. Padding and spacing on Create multisig account | `BUG-42.24.19-12` + `-13` | P3 ×2 |
| [x] | 21. Multisig confirmation screens use the old layout, not the Extension's | `BUG-42.24.7-01` | P2 |
| [ ] | 22. Tab bar cannot be scrolled sideways to reach the hidden tabs | *none logged* | — |
| [ ] | 23. History screen UI — dead filter button, filter-row colour, dropdown title colour | *none logged* (three bugs in the body) | — |
| | *(an empty checkbox follows — item 24 is mid-write)* | | |

**19 of 23 done.** Re-read 2026-09-09 **04:50 UTC**, against an issue whose own `updated_at` was
04:46. Nineteen ticks, unchanged since 03:31 — **the movement since the last read is entirely on the
open side**: items **22** and **23** were added at 04:46, and an empty checkbox after them says a
24th is coming. Open: **6** (`BUG-42.24.19-03`, P2), **9** (`BUG-42.24.19-06`, P1), **22** and
**23**. The two oldest items are still the two oldest, and are no longer the only two.

**The "two items left" reading held for about seventy-five minutes.** The entry below it was written
at 03:31 and closed with *"it has only now fallen, because nothing new arrived"* — a count that was
true when measured and stale by breakfast. The tracker is an open list on a build under test; a
falling count is not a trend, and this note has now twice written one up as if it were.

**Items 22 and 23 arrived with no bug, no report and no failed AC — and still have none.** Every
earlier item on this tracker was raised from a run and already had a row in a story before it
appeared here. These two came the other way round: a tracker comment first, nothing behind it. There
is **no 09-09 test report, no REG line marked failed, no bug row in any story, and nothing in
US-42.24.20**. The tracker comment is the entire evidence they have.

**This reverses the gap this note has tracked all week.** Until now the problem was bugs with no
checkbox — `BUG-42.24.2-04` and `-05` sat fixed and untracked for days. Items 22 and 23 are the first
of the opposite kind: **checkboxes with no bug**. Four bug rows were drafted here on 09-09 and
withdrawn the same day — allocating IDs and severities is the tester's call, the same boundary as the
test reports and US-42.24.20, and the tracker has never carried a severity for anyone to copy. So
this note names what the items say and stops there; the four descriptions below are the record until
the tester opens rows for them.

**Item 23 is the fifth merged checkbox, and the widest yet.** Its title — *"Please bug's UI the
history's screen"* — names one screen and one thing. Its collapsed body names **three**, on two
different features: the filter button that is still shown on the **Multisig** tab of History where it
does nothing — which is **US-42.24.7's** bug, not a regression one — the wrong colour behind the
filter row, and the wrong colour on the Select account dropdown title. Read on its title alone it is
a cosmetic History ticket; read on its body it reaches into the multisig story. **Items 2, 15, 18
and 20 all hid something the same way** — this is the fifth, and the first caught before the tick
rather than after it.

**Item 22 does not say what screen it is on.** *"The tab bar cannot be scrolled horizontally to view
the hidden tabs"* — the evidence is a video and no text names the screen. Item 23 puts a Multisig tab
inside History, so History's tab bar is the obvious guess, and a guess is exactly what it would be.
**The screen has to come off the video before the retest can check it**, or item 22 verifies against
whatever the retester happens to open.

**Correction — `BUG-42.24.7-01` was on the tracker, and this note said it was not.** It is **item
21**, posted 2026-09-08 at 11:05 UTC, and yesterday's entry called it *"the one logged bug with no
checkbox"*. The comment was already there when that was written. The cause is mechanical: the tracker
had been a single comment for six days, the read script indexed `comments[0]`, and when the tester
opened a **second comment** to add an item after the list had closed, the script kept reporting
twenty items and no error. **A negative finding needs the same corroboration as a positive one** —
`LESSONS §69` in the same shape as the ChainList PR miss: the query looked in one place and the
answer was reported as if it had looked everywhere.

**And the timestamp on yesterday's entry was not measured.** It reads *"Re-read 2026-09-08 17:20
+07"*; the issue's own `updated_at` at that fetch was 11:05 UTC — 18:05 +07 — so the read cannot have
happened before then. The time was carried over from an earlier `date` call instead of a new one.
Stamp a read with the clock, or do not stamp it.

**Every open bug is on the tracker; two tracker items have no bug.** All 26 logged bugs map to a
checkbox except two — and **both of those are already fixed**:

| Bug | | |
| --- | --- | --- |
| `BUG-42.24.2-04` | **fixed** | XCM could not fetch a fee on any route. Never a checkbox |
| `BUG-42.24.2-05` | **fixed** | Buying through Banxa never opened the supplier. Never a checkbox |

Neither word — *XCM*, *Banxa*, *buy*, *fee*, *supplier* — appears anywhere in either comment. Both
were logged in the same [09-05 report](../tests/test-reports/2026-09/2026-09-05/report-manual.md) and
both are among the **four** bugs US-42.24.20 has verified fixed. So half of everything verified fixed
never passed through #2057 at all: the tracker is a complete list of what is **outstanding**, and has
never been a list of what was **wrong**.

**Item 21's two screenshots, read field by field.** The bug is a comparison, so the evidence is the
pair: Mobile *actual*, Extension *expected*. Both screens show **the same eight fields** — nothing is
missing on either side, and the values differ only because the two captures are different transfers
(0.1 vs 0.01 DOT, different recipient, different call data), so they say nothing about formatting.
What differs is where the fields sit and what they are called:

| | Mobile | Extension |
| --- | --- | --- |
| Block 1 | Send from · **Send to** · Network | **Multisig** · Network |
| Block 2 | Amount | **Recipient** · Amount |
| Block 3 | Sign with · Multisig deposit · Network fee · Call data | *same* |
| Button | **Back** | **Go back** |

**The bug names two of the four differences.** It has *"Mobile labels the first row Send from"* (the
`Multisig` rename) and *"puts the recipient and amount in separate blocks"* (the recipient moving in
beside Amount). It does **not** say that `Send to` is renamed **Recipient**, and it does not mention
the **Back / Go back** button label at all. Its remaining phrase — *"the Extension … groups network
with it"* — describes something **both** sides do, since Network is in block 1 on Mobile too, so it
does not separate them.

**And the report holds only half the evidence.** `img/BUG-42.24.7-01.png` is the Mobile capture
alone; the Extension screen it is supposed to be compared against exists only in the tracker comment.
A bug whose entire content is *"does not match the Extension"* cannot be rechecked from the report on
its own — the same shape as item 18, where the tracker body carried more than any record of it.

**Item 21 was ticked with all of that unresolved.** `BUG-42.24.7-01` still reads `todo` in US-42.24.7
and *not fixed yet* in US-42.24.20, and the expected result says the fix *"applies to every multisig
confirmation screen, not only transfer"* — while the only evidence either side holds is the Transfer
screen. What the tick covers on the other confirmation screens is not written down anywhere.

**Item 20 was ticked without being split, and the residue went with it.** Yesterday this note called
item 20 *"the first chance to split a merged checkbox before it costs anything"*. It was ticked
whole at 02:22 UTC, so one tick now claims `BUG-42.24.19-12` (Select account headings) and
`BUG-42.24.19-13` (Create multisig blocks) on two different screens — and also the *"missing left and
right padding"* in its title, which still has no bug of its own. Third merged checkbox, third time
the record cannot say which half was fixed.

**Both bugs behind item 20 still read `todo`.** So do `BUG-42.24.19-11` behind item 19. Three P3s
filed on 09-08 are ticked as fixed on the tracker and open in the tester's record, and the 09-08
report's build line is still *"to fill in"*.

**Item 18's body is wider than every record of it.** The tracker adds *"Same issues: in all confirm
screen"* under the History-screen title, but `BUG-42.24.19-10`, its US-42.24.19 row and the 09-07
report all describe **only** the History screen. So the tick claims a fix across the confirm screens
that nothing on the tester's side is set up to check. Not a correction to make in the report — that
is the tester's record — but the retest needs the confirm screens named, or half of item 18 gets
verified by nobody.

**The proxy set is now closed out entirely.** Items 13–17 carry every bug US-42.24.5 logged —
`BUG-42.24.5-01` through `-06`, six bugs across five checkboxes — and all five are ticked. That is
the first sub-task whose whole bug list has been claimed fixed.

**Item 15 closed two bugs, not one** — its collapsed body asks for three things: the greyed-out
colour, the missing dropdown arrow (both `BUG-42.24.5-03`) *and* **"Display goverance option when
mobile isnot supported governance function"**, which is `BUG-42.24.5-04`. The tester filed the two
off one screenshot (`img/BUG-42.24.5-03.png`). Ticking item 15 therefore claims both.

**Four items left, and the two oldest are still there.** 6 (`BUG-42.24.19-03`, Done-button layout
on iOS 26.6.1, P2) and 9 (`BUG-42.24.19-06`, P1), joined on 09-09 by 22 and 23. The count sat at
three for three days while items kept arriving and being ticked — 09-08 09:59 ticked 19 and added 20
in one edit, 11:05 added 21, 09-09 02:22 ticked 20 and 03:31 ticked 21 — fell to two at 03:31, and
was back to four by 04:46. **6 and 9 have been open since the tracker was written on 09-03**, and
**every item filed between them and 09-09 has been ticked**. Items 20 and 21 went from filed to
ticked in about sixteen hours each; these two have sat for six days. Items 22 and 23 are hours old
and prove nothing yet. **Item 9 is the only P1** — intermittent infinite loading after
reopening — and being intermittent it is both the hardest to confirm fixed and the one most likely
to come back as *cannot reproduce*. Item 6 is a button layout on one iOS version. Neither is hard to explain away, which is
probably why both are still here.

**The tester's records lag the tracker by design.** 19 items are ticked while US-42.24.20 marks only
four bugs verified — `BUG-42.24.1-01`, `BUG-42.24.19-01`, `BUG-42.24.2-05` and now `BUG-42.24.2-04` —
and the other 22 *not fixed yet*. Verification happens in
[US-42.24.20](../sprints/stories/US-42.24.20-qc-web-runner-verify-bugs.md), on a build, and that is
the tester's call. A tick here means the developer says it is fixed; it is not the same claim.

### Logged but never put on the tracker

**None outstanding.** For the first time since this note was opened, every bug that is still open has
a checkbox. Three left this list in three different ways:

| Bug | Sev | Went | |
| --- | --- | --- | --- |
| ~~`BUG-42.24.7-01`~~ | P2 | **tracked** | Added as **item 21** on 09-08 at 11:05 UTC — in a second comment, which is why this note missed it. Still `todo`, still unticked |
| ~~`BUG-42.24.2-04`~~ | ~~P1~~ | **fixed** | Verified in US-42.24.20 — the PAH → Energy Web X route sends on 1.3.72. Fixed without ever being tracked |
| ~~`BUG-42.24.4-01`~~ | ~~P1~~ | **withdrawn** | Dropped 2026-09-08 as an Extension issue, not a web-runner one. AC-16 now passes and US-42.24.4 closes 39 of 39 with no bugs |

`BUG-42.24.2-05` (Banxa, P2) also never appeared here and never appeared on the tracker; it was
already fixed and verified before this section was written, which is why it was never missed.

**Two corrections, one method.** `BUG-42.24.5-04` was called untracked four times — it is the third
bullet inside item 15's collapsed **body**, and the title (*"Display incorrect color of option type
proxy"*) never mentions Governance. `BUG-42.24.7-01` was called untracked once — it is item 21, in
the tracker's second **comment**. Both misses come from reading a part of the source and reporting on
the whole of it: first the titles without the bodies, then one comment without the other. The same
error made this note call items 8 and 9 unlogged when they were `BUG-42.24.19-05` and `-06`.
**Expand every body, read every comment, and only then say a thing is not there.**

**`BUG-42.24.2-04` is fixed, and the tracker never knew it existed.** This note called it the most
severe open item in the set for two days running; US-42.24.20 now records the PAH → Energy Web X
route sending on 1.3.72. It was logged in the 09-05 report and fixed by 09-08 without ever being one
of the things #2057 asks for — so the tracker is not a complete list of what was wrong, only of what
was routed through it. Two AC still wait on a rerun: **AC-10** (US-42.24.2) and **REG-20**
(US-42.24.19).

**But its bug rows still say `todo`.** US-42.24.20 marks it *fixed*, while its own row in US-42.24.2
and the carried row in US-42.24.19 both still read `todo` — and US-42.24.19's test notes say *"the
defect is fixed"* two lines below its own `todo`. The tester's record disagrees with itself in three
places. Not this note's to fix, but the retest cannot be read off the bug tables while it holds.

**`BUG-42.24.4-01` was withdrawn, not fixed.** The Subscan API key bug was reclassified as an
Extension issue on 09-08 and removed from US-42.24.4, which now closes 39 of 39 with no bugs. It no
longer exists anywhere in `docs/` — **this note was the last place still carrying it as an open P1**,
which is exactly the failure mode of a note that summarises records it does not own.

## Open

- [x] ~~`BUG-42.24.7-01` (P2) is not on the tracker~~ — **wrong**; it is item 21, added 09-08 in the
  tracker's second comment. It is still `todo` and still unticked, but it is tracked.
- [ ] **Read both comments, and check the second one is finished** — the tracker outgrew one comment
  on 09-08 and nothing announces that. Any check that indexes the first comment reports twenty items
  and no error. As of 09-09 04:46 the second comment ends in an **empty checkbox**: item 24 is being
  typed, so any count taken now is a snapshot of an edit in progress.
- [ ] **Items 22 and 23 have no bug row anywhere** — first checkboxes on this tracker with nothing
  behind them: no bug ID, no severity, no test report, no failed REG line, nothing in US-42.24.20.
  Opening the rows is the tester's call; until then there is no record to tick them against.
- [ ] **Item 22 names no screen** — the screen is only in the video. Name it before the retest, or the
  item verifies against whatever screen the retester happens to open.
- [ ] **Item 23 is a merged checkbox — split it before it is ticked** — three bugs in one body, and
  one of them (the dead filter button on the Multisig tab) belongs to US-42.24.7, not the regression
  story. Items 15, 18 and 20 were each ticked whole and the record cannot now say which half was
  fixed. This is the first one caught early enough to matter.
- [ ] **`BUG-42.24.2-04`'s status contradicts itself** — *fixed* in US-42.24.20, `todo` in both
  US-42.24.2 and US-42.24.19, whose own notes then say the defect is fixed. One of the three is wrong.
- [ ] **AC-10 and REG-20 still need rerunning** — the XCM defect is gone but neither AC has been
  re-run, so no AC yet records that XCM works.
- [x] ~~Item 20 is a merged checkbox — split it before ticking~~ — **too late**; ticked whole
  2026-09-09 02:22 UTC. One tick now claims `BUG-42.24.19-12` and `-13` on two different screens.
- [ ] **Item 20's left/right padding still has no bug, and is now claimed fixed** — `-12` logs only
  the heading padding. Read off the screenshots, not the tester's word; confirm before relying on it.
- [ ] **Items 19 and 20 are ticked while `BUG-42.24.19-11`, `-12` and `-13` all say `todo`** — three
  P3s filed 09-08, ticked as fixed, open in the tester's record. A build number would say whether
  that is lag or a disagreement.
- [x] ~~`BUG-42.24.5-04` is not on the tracker~~ — **wrong**; it is inside item 15, now ticked.
- [x] ~~Item 19 was ticked with no bug ID~~ — logged 2026-09-08 as `BUG-42.24.19-11` (P3).
- [x] ~~Item 20 has no bug ID~~ — logged 2026-09-08 as `BUG-42.24.19-12` and `-13` (P3 each).
- [x] ~~`BUG-42.24.4-01` (P1) is not on the tracker either~~ — **withdrawn** 2026-09-08 as an
  Extension issue; it is no longer a bug in this set at all.
- [ ] **Item 9 is intermittent** (*"sometimes"*) — it needs a reproduction rate before it can be
  worked, or it will bounce back as *cannot reproduce*.
- [ ] **Build identity** — the 09-03, 09-04 and 09-08 reports all still read *"to fill in"*. This has
  stopped being tidiness: three bugs filed on 09-08 are now ticked as fixed and open in the tester's
  record at once, and without a build on that report nothing says which build the tester was on.
  `6de4df1a4d` puts the bundle at **1.3.89-0**. Diawi links expire.
- [x] ~~Item 21 has never been ticked~~ — ticked 2026-09-09 03:31 UTC, one day after filing. It is
  still the only item in the second comment, so it stays the one most easily missed.
- [ ] **Item 21's tick covers more than anything on record** — `BUG-42.24.7-01` names two of the four
  differences its own screenshots show (not the `Send to` → `Recipient` rename, not `Back` /
  `Go back`), and its expected result claims *every* multisig confirmation screen while the only
  evidence is the Transfer one. What was fixed on the other screens is written down nowhere.
- [ ] **The report holds only the Mobile half of `BUG-42.24.7-01`** — `img/BUG-42.24.7-01.png` is the
  actual; the Extension screen it is compared against lives only in the tracker comment. A comparison
  bug needs both captures in the report, or it cannot be rechecked from the report.
- [ ] **Confirm item 1** was the web-runner bump and not something else.
- [x] ~~Retest story for US-42.24.1~~ — exists as
  [US-42.24.20](../sprints/stories/US-42.24.20-qc-web-runner-verify-bugs.md); AC-7/8/9 rerun and pass.
- [x] ~~Item 4 needs an ID and a home~~ — `BUG-42.24.19-01` under US-42.24.19.

## Worth keeping

- **`BUG-42.24.2-03` was a missing feature, not a regression** — `da0fbbcbae` is *"**feat**: add the
  token price chart"*. It was filed under US-42.24.2 (chain-list v0.2.122) and belonged to neither
  that version nor any other.
- **Most findings land outside the version that caught them.** `-02`, `-03` and items 4–9 are app-wide
  or first-run bugs, not chain-list ones — which is why 4–7 ended up on US-42.24.19 (regression)
  rather than on a version sub-task. A version-by-version backfill sweeps the whole app, so expect
  this to keep happening.
- **Merged checkboxes have now cost three times, and the third was foreseen.** Item 2 held two bugs
  behind one checkbox and was ticked without being split, so which one it closed was unreadable; item
  15 held `BUG-42.24.5-03` and `-04`, and because `-04` appears only in the collapsed body this note
  called it untracked four times; item 20 held `BUG-42.24.19-12` and `-13` across two screens, was
  named here as the one chance to split a merged item before it cost anything, and was ticked whole
  the next morning. **A checkbox is a title plus a body, and only the body is complete.** Naming the
  risk in a note nobody reads before ticking does not prevent it.
- **A note that summarises records it does not own goes stale silently.** `BUG-42.24.4-01` was
  withdrawn and `BUG-42.24.2-04` was fixed, both on 09-08, and this note carried both as open P1s
  until it was re-read against the stories. #2057 itself had not moved at all — the tester's docs
  had. **Re-read both sides, not just the tracker.**

*Boundary: none of this is an Extension code change, requirement or release item. Canonical work
stays in [SubWallet-Mobile](https://github.com/Koniverse/SubWallet-Mobile) — same as
[notes/2026-07-30.md §A](2026-07-30.md).*
