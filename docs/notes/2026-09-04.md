# Notes — 2026-09-04

QC tracker for [SubWallet-Mobile #2057](https://github.com/Koniverse/SubWallet-Mobile/issues/2057)
(*Update web-runner 1.3.86*). Findings from the 2026-09-03 session —
[report-manual.md](../tests/test-reports/2026-09/2026-09-03/report-manual.md).

**Tick a box here when the tracker box is ticked.** Fix status is dev-side and lives here, not in the
test report.

## Bugs

| ✔ | Item | Bug | Sev | Fix |
| --- | --- | --- | --- | --- |
| [x] | 1. NFT ERC-721 not auto-detected on import/attach | `BUG-42.24.1-01` | P1 | no commit — likely the web-runner bump (`ddae4610e9`, `6de4df1a4d` → 1.3.89-0) |
| [x] | 2. Network toggle / disconnected badge | `BUG-42.24.2-01` + `-02` | P1 ×2 | `140b58a8fd` |
| [x] | 3. Price chart missing on token detail | `BUG-42.24.2-03` | P2 | `da0fbbcbae`, `59869ed481` |
| [ ] | 4. "Migrate to Unified Account" shown twice on import (iOS) | *no ID yet* | ? | — |

All fixes are commits on [PR #2058](https://github.com/Koniverse/SubWallet-Mobile/pull/2058), not
separate PRs.

## Open

- [ ] **Retest story for [US-42.24.1](../sprints/stories/US-42.24.1-qc-web-runner-1-3-68.md)** —
  AC-7, AC-8, AC-9 need rerunning on a build with the bump. The story stays `blocked` and is not
  edited: it is the record of the 09-03 build, and says so itself.
- [ ] **Item 4 needs a bug ID and a home.** Not chain-list, so US-42.24.2 is the wrong owner. Check
  whether the migration runs twice or only the screen renders twice
  ([LESSONS §71](../LESSONS.md)) — if the former, it is not P2-shaped.
- [ ] **Build identity** — the report still reads *"to fill in"*. Four bugs hang off an unnamed
  build; `6de4df1a4d` puts the bundle at **1.3.89-0**. Diawi links expire.
- [ ] **Confirm item 1** was the web-runner bump and not something else.

## Worth keeping

- **`BUG-42.24.2-03` was a missing feature, not a regression** — `da0fbbcbae` is *"**feat**: add the
  token price chart"*. It was filed under US-42.24.2 (chain-list v0.2.122) and belonged to neither
  that version nor any other.
- **Three of five findings landed outside the story that caught them** (`-02`, `-03`, item 4). A
  version-by-version backfill sweeps the whole app, so it keeps finding things belonging to no
  version in the queue. They need somewhere to go that is not the sub-task that happened to be open.
- **Item 2 held two bugs behind one checkbox** and was ticked without being split, so which one it
  closed was unreadable from the tracker; both were confirmed fixed by asking the developer. Split
  merged items before ticking, or the record stops being reconstructable from the record.

*Boundary: none of this is an Extension code change, requirement or release item. Canonical work
stays in [SubWallet-Mobile](https://github.com/Koniverse/SubWallet-Mobile) — same as
[notes/2026-07-30.md §A](2026-07-30.md).*
