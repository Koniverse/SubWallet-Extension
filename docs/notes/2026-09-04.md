# Notes — 2026-09-04

QC tracker for [SubWallet-Mobile #2057](https://github.com/Koniverse/SubWallet-Mobile/issues/2057)
(*Update web-runner 1.3.86*). **Updated 2026-09-15 04:51 UTC** — **51 of 57 done**.

**Tick a box here when the tracker box is ticked.** Fix status is dev-side and lives here; the
tester's own verification is [US-42.24.20](../sprints/stories/US-42.24.20-qc-web-runner-verify-bugs.md)
and is not mirrored here.

**Read every comment — there are three now.** Items 1–20 are in the
[09-03 comment](https://github.com/Koniverse/SubWallet-Mobile/issues/2057#issuecomment-5520087148),
items **21–48** in a [second one](https://github.com/Koniverse/SubWallet-Mobile/issues/2057#issuecomment-5584157750)
opened 09-08, and items **49–57** in a **third**, opened 09-14 03:38 UTC under the heading *"Version
test:"* — the tester's sweep of the backlog versions (1.3.78, then 1.3.80), still being written. Item 36 holds two sub-checkboxes of its own; they are
not items. Anything that reads `comments[0]` sees twenty items and no error — that is how
`BUG-42.24.7-01` was called untracked, and a script fixed to read two comments would now miss the
third.

## Bugs

| ✔ | Item | Bug | Sev |
| --- | --- | --- | --- |
| [x] | 1. NFT ERC-721 not auto-detected on import/attach | `BUG-42.24.1-01` | P1 |
| [x] | 2. Network toggle / disconnected badge | `BUG-42.24.2-01` + `-02` | P1 ×2 |
| [x] | 3. Price chart missing on token detail | `BUG-42.24.2-03` | P2 |
| [x] | 4. "Migrate to Unified Account" shown twice on import (iOS) | `BUG-42.24.19-01` | P2 |
| [x] | 5. Create-password screen freezes on fresh install | `BUG-42.24.19-02` | P1 |
| [ ] | 6. Done button layout on the Amount screen (iOS 26.6.1) | `BUG-42.24.19-03` | P2 |
| [x] | 7. NFT transfer on EVM refused — names the account, not the network | `BUG-42.24.19-04` | P1 |
| [x] | 8. Substrate NFTs not displayed | `BUG-42.24.19-05` | P1 |
| [ ] | 9. Infinite loading after reopening the app, intermittent | `BUG-42.24.19-06` | **P1** |
| [x] | 10. Crash tapping (+) when importing a custom network (Android) | `BUG-42.24.19-07` | P2 |
| [x] | 11. Wrong content on the (i) tooltip of the locked-balance Others row | `BUG-42.24.19-08` | P2 |
| [x] | 12. Account name indented on Create derived account (Android) | `BUG-42.24.19-09` | P2 |
| [x] | 13. Remove the Available balance field on Add proxy | `BUG-42.24.5-01` | P2 |
| [x] | 14. Call data cannot be scrolled on the Add proxy confirmation sheet | `BUG-42.24.5-02` | P2 |
| [x] | 15. Proxy type option — colour, missing dropdown, Governance offered | `BUG-42.24.5-03` + `-04` | P2 ×2 |
| [x] | 16. Double colon on the Available balance label, Remove proxy | `BUG-42.24.5-05` | P3 |
| [x] | 17. Remove proxy confirmation title truncated with space to spare | `BUG-42.24.5-06` | P3 |
| [x] | 18. Android back button dead on History **and all confirm screens** | `BUG-42.24.19-10` | P2 |
| [x] | 19. Spacing between sections on the Unstake screen | `BUG-42.24.19-11` | P3 |
| [x] | 20. Padding and spacing on Create multisig account | `BUG-42.24.19-12` + `-13` | P3 ×2 |
| [x] | 21. Multisig confirmation screens use the old layout, not the Extension's | `BUG-42.24.7-01` | P2 |
| [x] | 22. Tab bar on the multisig account details screen cannot be scrolled sideways | `BUG-42.24.7-02` | P2 |
| [x] | 23. History UI — dead filter button, filter-row colour, two wrong title colours | `-03` `-04` `-05` `-07` | P3 ×4 |
| [x] | 24. Pending record in the Multisig tab has no date heading | `BUG-42.24.7-06` | P2 |
| [x] | 25. Notification screen UI — Mark all as read overruns the right edge | `BUG-42.24.7-08` | P2 |
| [x] | 26. Navigation on tapping a notification — **multisig only**; claim unlogged | `BUG-42.24.7-09` | P2 |
| [x] | 27. Swap button is not disabled on a multisig account | `BUG-42.24.7-10` | P2 |
| [x] | 28. MANTA earning offered on multisig; **DOT single-option screen unlogged** | `BUG-42.24.7-11` | P2 |
| [x] | 29. Notification settings has no Pending multisig approvals option | `BUG-42.24.7-12` | P2 |
| [x] | 30. Multisig accounts have no multisig badge on their avatar | `BUG-42.24.7-13` | P2 |
| [x] | 31. Select account screen for a new multisig shows no account-type badges | `BUG-42.24.7-14` | P2 |
| [x] | 32. The account filter on Export account has no Multisig option | `BUG-42.24.7-15` | P2 |
| [x] | 33. Toast on a pending multisig transaction hidden behind the detail sheet — **ticked with a follow-up: spacing varies by device height** | `BUG-42.24.7-16` | P2 |
| [x] | 34. Multisig tab does not offer to enable a network that is turned off | `BUG-42.24.7-17` | P2 |
| [ ] | 35. View transaction does not open the transaction details | `BUG-42.24.7-18` | P2 |
| [x] | 36. Governance pending-multisig — **code check**; ticked with the *"Recheck Submit/Approve/Reject"* sub-box still open | *none — not a bug* | — |
| [x] | 37. Cross-chain transfer from a multisig refused with the wrong message | `BUG-42.24.7-19` | P2 |
| [x] | 38. Signatory validation message shown as a red block, not a tooltip — **ticked as declined: "requires a different mechanism on mobile, keep as is"** | `BUG-42.24.7-20` | P3 |
| [x] | 39. Multisig transaction detail sheet does not match the Extension | `BUG-42.24.7-21` | P2 |
| [x] | 40. Select an account to sign sometimes never finishes loading (dApp via multisig) | `BUG-42.24.7-22` | **P1** |
| [x] | 41. Pending Multisig popup — Sender/Recipient columns misaligned | `BUG-42.24.7-23` | P2 |
| [ ] | 42. Scrolling performance through Pending Multisig records — **deferred: "optimization scope still unclear, needs investigation"** | *none logged* | — |
| [x] | 43. Insufficient balance warning on Add proxy confirmation not styled like the Extension | `BUG-42.24.7-24` | P3 |
| [x] | 44. Creating a multisig that already exists as a watch-only account shows a raw error key | `BUG-42.24.7-25` | P2 |
| [x] | 45. Starting a second identical multisig transaction shows a raw error key | `BUG-42.24.7-26` | P2 |
| [x] | 46. "Unable to sign transaction" shown while the signatory list is still loading | `BUG-42.24.7-27` | P2 |
| [x] | 47. Signatory picker shown on a network that does not support multisig | `BUG-42.24.7-28` | P2 |
| [x] | 48. Subnet token cannot be transferred — form has no validator fields and no subnet name | `BUG-42.24.9-01` | **P1** |
| [x] | 49. Insufficient-balance popup on Start earning uses the system dialog, not the app's (1.3.78, #4803) | `BUG-42.24.11-01` | P2 |
| [x] | 50. Manage networks does not match the Extension after Disable all networks (1.3.78, #4970) | `BUG-42.24.11-02` | P3 |
| [x] | 51. Align the Available section and add the missing notification on Alpha / TAO transfer (1.3.78, #4900) | `BUG-42.24.11-03` | P3 |
| [x] | 52. Swap screen — `simple id: undefined` on confirmation **fixed**; token-name truncation **declined**: "may affect other devices and tokens" (1.3.78, #4899) | `-11-05` fixed · `-11-04` declined | P2 · P3 |
| [x] | 53. *Go to parent* button on a nested NFT has no button styling (1.3.80, #4884) | `BUG-42.24.13-01` | P3 |
| [ ] | 54. NFT information block differs from the Extension — empty panel + stray Resources row, and fields laid out differently; Normal and Nested NFT (1.3.80, #4884) | `BUG-42.24.13-02` + `-03` | P3 ×2 |
| [x] | 55. Info icon in the Description popup stretched down the height of the text — **the (i) icon's bar stretches the full text height with the icon pinned to the top; Extension centres a small icon on a short bar** (1.3.80) | *none logged* | — |
| [x] | 56. Unstake confirmation drops the TAO unstaking-fee notice — **ticked as push-back: "Recheck, native staking have no notice"** | *none logged* | — |
| [ ] | 57. *"Please check the issue in [#2053](https://github.com/Koniverse/SubWallet-Mobile/issues/2053)"* — a **second tracker** (react-native 0.82.1, May 2026) with four open sub-items | *none logged* | — |

Open: **6, 9, 35, 42, 54, 57** — six.

**Item 38 was ticked as a decision, not a fix.** Next to the box: *"On mobile, it requires a
different mechanism, so we'll keep it as is instead of changing it to a tooltip."* So `BUG-42.24.7-20`
(P3) is **declined** — the red block stays. That is a legitimate close, but it is a different claim
from every other tick on this list, and the tester's row should end as *won't fix* rather than
*fixed* or the retest will look for a tooltip that was never going to come. Second tick to carry its
own caveat, after item 33.

**Item 40 is ticked — the signing-list P1.** `BUG-42.24.7-22`, *Select an account to sign* sometimes
never finishing on a dApp request via multisig. Ticked 09-14 without a caveat, three days after item
46 (the error shown while that same list loads) was ticked ahead of it. **That leaves one P1: item
9**, intermittent infinite loading after reopening, open since 09-03 — the oldest item on the list
and the only one that has never moved. Because item 40 was also *"sometimes"*, its retest needs more
than one attempt before `-22` can read *fixed*.

**The multisig round is closed on the tracker.** Items 21–47 are all ticked except 35 (`-18`, View
transaction does not open), 36 (a code read, not a bug) and 42 (scroll performance, no bug). Of
twenty-seven logged multisig bugs, **twenty-six have a ticked checkbox** and all twenty-seven still
read `todo` on the tester's side.

**Item 36 is ticked while half of it is not.** Its body holds two sub-checkboxes: the Notion content
spec (ticked) and *"Recheck the Submit, Approve, and Reject actions for Pending Multisig in the
Governance feature"* (**not ticked**). The parent box was ticked 09-14 anyway. Since 36 was a
code-read request in the first place, the tick most likely means *the spec was read*; the Governance
recheck it was actually asking for is still open by its own checkbox. Fourth annotated-or-partial
tick after 33, 38 and 42 — and the only one where the tracker itself records the residue.

**The third comment is a sweep of the backlog versions, and the tester's records caught up the same
afternoon.** 49–52 are 1.3.78 (#4803, #4970, #4900, #4899); 53–54 are 1.3.80 (#4884, Unique Network
NFTs). The 09-14 session (PR #5081) closed
[US-42.24.11](../sprints/stories/US-42.24.11-qc-web-runner-1-3-78.md) with five bugs and
[US-42.24.12](../sprints/stories/US-42.24.12-qc-web-runner-1-3-79.md) (1.3.79) with none, ran most
of [US-42.24.13](../sprints/stories/US-42.24.13-qc-web-runner-1-3-80.md) with three, and put all
three stories on **W38**. Every item in the third comment now has a row — eight bugs across six
checkboxes. 49, 50 and 51 were ticked before their rows existed; all eight rows read `todo`.

**Item 52 was ticked with the split written in — the first merged checkbox closed honestly.** The
tester's rows had made it two bugs: `BUG-42.24.11-04` (alpha token names cut to *"Bittens…"*, P3) and
`BUG-42.24.11-05` (a raw address line and **`simple id: undefined`** on the confirmation, P2). The
tick on 09-15 carries *"The name displayed on the swap screen will remain unchanged because this may
affect other devices and tokens"* — so `-11-04` is **declined** and the tick claims `-11-05`. That is
what every earlier merged tick lacked: one line saying which half. The tester's rows should end as
*won't fix* and *fixed* respectively, not both *fixed*. Item 54 is still two (`-13-02` empty panel +
Resources row, `-13-03` layout) and still unticked; its body now shows Normal and Nested NFT pairs.

**Item 55 is the (i)-icon stretch, split out of 54 by the tester on 09-15.** The Description popup's
info icon runs the full height of the text with the glyph pinned to the top, where the Extension
centres a small icon on a short bar. It had been folded into 54's screenshots; now it is its own
box, which is the right shape — and it has **no bug row**: `-13-02` is the empty panel and Resources
row, `-13-03` is field layout, neither is this. Item 53 (`-13-01`, *go to parent* styling) ticked
09-15; item 55 ticked an hour later, still with no row.

**Item 56 was filed and pushed back in the same edit.** *"The unstake confirmation screen drops the
TAO unstaking fee notice the Extension shows"* — ticked with *"Recheck, native staking have no
notice"*: the claim is that the Extension shows no such notice on native staking either, so there is
nothing to match. Third tick that is not a fix (after 38 declined and 52 half-declined), and the
first that disputes the finding rather than the remedy. If the tester agrees, it should not become a
row; if they do not, it is a bug with a tick already on it.

**Item 57 is a pointer to another tracker.** Its whole text is *"Please check the issue in
#2053"* — [SubWallet-Mobile #2053](https://github.com/Koniverse/SubWallet-Mobile/issues/2053),
*"Please check some issue when update react-native version 0.82.1"*, opened **2026-05-11** and still
open, with **four unticked sub-items** of its own: Face ID prompting twice on unlock (iOS), a loading
spinner on every keystroke in name and amount fields across create / import / attach / change-stake,
no error on an invalid QR across six scan entry points, and the wrong account shown after deleting a
migrated one. One checkbox here, four there, none logged anywhere in `docs/`, and none of it is
web-runner content. **Ticking 57 means ticking #2053**, and #2053 has its own boxes.

**Item 42 is now explicitly deferred, unticked.** The developer wrote under it on 09-14: *"We'll
follow up and update if have any feedback later, as the optimization scope is still unclear and
requires further investigation before we can identify the right approach."* That is the third
annotated checkbox after 33 (fixed with a residue) and 38 (declined), and the first that is neither
— an open item with a stated reason for staying open. It has no bug row and no measurable claim, so
there is nothing to log until someone names what *smooth* means on which device.

**A third comment opened 09-14 03:38 — the 1.3.78 round has started.** Items 49 and 50 are both
1.3.78 content: the Start-earning insufficient-balance popup using the system dialog (#4803, liquid
staking bridge fee) and Manage networks not matching the Extension after *Disable all networks*
(#4970). That is [US-42.24.11](../sprints/stories/US-42.24.11-qc-web-runner-1-3-78.md)'s scope, and
the story still reads `backlog` with no sprint; no bug rows yet.

**Nineteen ticks since 09-11, and the list grew again today.** 09-11: 24, 26, 30, 31, 39, 41, 22,
46, 47 (+48), 32, 33, 34, 37, 44, 45; 09-14: 38, 48, 40, 43 (+49, +50). Open went 3 → 2 → 4 → 7 →
9 → 23 → 21 → 16 → 15 → 12 → 9 → 8 → 7 → 7. Seventeen of the nineteen are `BUG-42.24.7-*` rows. **Items 21–34 and
37–39, 41, 44–47 are ticked**; what is left of the multisig round is 35, 36, 40, 42, 43 — and of
those, 36 is a code read and 42 has no bug, so **three multisig defects with a row remain**: 35
(`-18`), 40 (`-22`, P1), 43 (`-24`).

**Items 44 and 45 were the two raw-error-key bugs** — `-25` and `-26`, both cases where the app
showed an untranslated key instead of the sentence the tracker asked for. Both ticked together, which
is what you would expect if the fix was the missing translation rather than two separate handlers.
The retest should read the actual sentence on each, not just that a key no longer shows.

**Item 33 is the first tick that says so itself.** The developer wrote *"Will follow up since device
heights vary, which may cause some excess spacing"* next to the box — a tick with a declared residue,
which is more than any of the merged checkboxes ever carried. It still means `-16` needs a retest
on more than one device height, and that the tick is not a claim of done.

**Item 46 was ticked and item 40 was not — and they are the same list.** `-27` is *"Unable to sign"*
shown while the signatory list is still loading; `-22` (item 40, **P1**) is that list sometimes never
finishing. Fixing the premature error without fixing the load leaves the user with a spinner instead
of a wrong message. The tick on 46 says nothing about 40 until 40 is ticked too.

**Item 48 was the first non-multisig item since 09-08, a third P1, and is now ticked.** A subnet
token's transfer form had no validator fields and no subnet name, so the transfer could not be
completed — the Alpha-token path from 1.3.78 (#4900). Logged 09-11 as `BUG-42.24.9-01` under the
**1.3.76** story (US-42.24.9), P1, still `todo` on the tester's side; ticked 09-14.

**One P1 left.** Item 9 (`BUG-42.24.19-06`, infinite loading after reopening, *"sometimes"*), open
since 09-03. Items 40 (`BUG-42.24.7-22`, signing list) and 48 (`BUG-42.24.9-01`, subnet transfer)
were the other two and both closed 09-14.

**The second comment is the multisig round, and the tester's records lead it now.** Twenty-seven
items, all multisig, notifications or dApp-signing-via-multisig — areas US-42.24.19 and US-42.24.5
do not cover. `BUG-42.24.7-02` … `-28`, **twenty-seven bugs**, logged across the 09-09 and 09-10
sessions, one per line of body. Two items have none and should not: **36** is a request to read the
code for a Governance path the unstable testnet would not exercise, and **42** is scroll performance
with no measurable claim. Everything else has a row.

**Three residues, where the checkbox says more than the bug — and two are now ticked.** Item 26's
body holds two videos, claim and multisig; only the multisig one is logged (`-09`) and the item was
ticked whole on 09-11. Item 28's body adds *"the DOT staking option screen is shown even with only
one option"* on top of MANTA (`-11`), ticked 09-09. Item 42 has no bug. So the claim-notification
navigation and the DOT single-option screen are both **claimed fixed with nothing to verify them
against**; the retest will pass them without looking. Opening rows is the tester's call.

**Nothing logged is untracked any more.** `BUG-42.24.2-04` (XCM, every route) and `-05` (Banxa) were
fixed **without ever being a checkbox**; `BUG-42.24.4-01` was withdrawn 09-08 as an Extension issue;
`BUG-42.24.7-01` was item 21 all along. **The tracker lists what is outstanding, never what was
wrong** — half of everything US-42.24.20 verifies fixed never passed through #2057.

**Item 54, read off the screenshots (09-14).** The tracker gives two images and no words. The
difference is the info icon's container on the NFT description sheet: on Mobile the (i) sits at the
top of a dark-navy bar that runs the **full height** of the description text; on the Extension the
(i) is smaller, **vertically centred** against the text, and its bar is only as tall as the icon.
The text block itself is the same on both. Still reproduces after the 09-14 ticks — checked against
the Extension's *KodaDot: Arts DAO Fest 2024* block on the same date. One line of words so the
retest has something to compare against besides two pictures.

**A tick is not a verification.** 51 items ticked; US-42.24.20 marks four bugs verified and the rest
*not fixed yet*. A tick means the developer says it is fixed. **All twenty-seven multisig bugs are
`todo` while twenty-six of their checkboxes are ticked**.

## Open

- [x] ~~Items 22 and 24–28 have no bug row~~ — logged 2026-09-09 as `BUG-42.24.7-02` … `-15`;
  items 33–47 followed on 09-10 as `-16` … `-28`. Only **36** (a code check) and **42** (scroll
  performance) have none, and neither is a defect with a reproduction.
- [x] ~~Item 22 names no screen~~ — the tester named it: the **multisig account details** screen.
- [x] ~~Split item 23~~ — split on the tester's side into `-03`, `-04`, `-05`, `-07`, though the
  checkbox was ticked whole, so the tick still does not say which of the four it closed.
- [ ] **Items 26 and 28 are ticked and half of each is unlogged** — `-09` covers the multisig
  notification, not the claim one in item 26's second video; `-11` covers MANTA, not the *"DOT
  staking option screen shown with only one option"* in item 28's body. Both halves are claimed
  fixed against nothing.
- [ ] **Item 18's confirm screens, item 21's other multisig screens** — both ticks claim more than
  any record describes. The retest needs those screens named or the extra half is verified by nobody.
- [ ] **`BUG-42.24.2-04`'s status contradicts itself** — *fixed* in US-42.24.20, `todo` in
  US-42.24.2 and US-42.24.19. **AC-10 and REG-20 still need rerunning** so an AC records XCM working.
- [ ] **`BUG-42.24.19-11`, `-12`, `-13` say `todo`** while items 19 and 20 are ticked fixed. So do
  **all twenty-seven `BUG-42.24.7-02…-28`**, twenty-six of whose checkboxes are ticked. A build number on
  the reports would say whether that is lag or disagreement.
- [x] ~~Item 40 is a P1 on a signing path, ticked behind its neighbour~~ — ticked 09-14. Retest `-22`
  more than once; it was *"sometimes"*.
- [ ] **Item 42 is deferred pending investigation** — no bug, no target. If it is to be worked, it
  needs a device, a record count and a frame-rate or jank measure before anyone can tick it honestly.
- [ ] **Item 36's Governance recheck is still open under a ticked parent** — the sub-box says so.
  Do not read 36 as done until *Submit / Approve / Reject on a pending Governance multisig* has been
  exercised on a testnet that submits.
- [x] ~~Items 49–54 have no bug row~~ — logged 09-14 as `BUG-42.24.11-01…05` and
  `BUG-42.24.13-01…03`. 49, 50, 51 were ticked before the rows existed; all eight read `todo`.
- [x] ~~Item 52 says nothing in words / split before ticking~~ — ticked 09-15 **with** the split:
  `-11-05` fixed, `-11-04` declined (*"may affect other devices and tokens"*). Tester's rows should
  read *fixed* and *won't fix*, not both *fixed*.
- [ ] **Item 54 is two bugs behind one box** — `-13-02` and `-13-03`. Unticked; split it now.
- [ ] **Items 55 and 56 have no bug row and are both ticked** — 55 (icon stretch) as a fix, 56
  (unstake-fee notice) as a push-back. 56 should either not become a row or become one with the
  tick already disputed.
- [ ] **Item 57 delegates #2053 — four sub-items, four months old, none logged.** Decide whether
  #2053 is in this programme's scope; if it is, its four boxes need rows, and 57 cannot be ticked
  until they are.
- [ ] **Two declined-by-tick rows need *won't fix*, not *fixed*** — `BUG-42.24.7-20` (item 38, no
  tooltip on mobile) and `BUG-42.24.11-04` (item 52, token names stay truncated). Tester's rows to
  change; otherwise the retest looks for a change that was decided against.
- [ ] **Item 33 was ticked with a follow-up** — spacing varies by device height. Retest `-16` on more
  than one device, or the tick reads as done when the developer said it was not.
- [x] ~~Item 48 has no bug row~~ — logged 09-11 as `BUG-42.24.9-01`, **P1**, on the 1.3.76 story
  (US-42.24.9). The tester filed it under subnet naming, not the 1.3.78 Alpha-transfer sub-task;
  either is defensible, and it is their call.
- [ ] **Item 36 wants a code read, not a fix** — Governance pending-multisig Submit/Approve/Reject,
  which the unstable testnet would not exercise. The tick, if it comes, will not be a fix.
- [ ] **Build identity** — the 09-03, 09-04 and 09-08 reports all read *"to fill in"*. `6de4df1a4d`
  puts the bundle at **1.3.89-0**. Diawi links expire.
- [ ] **Item 9 needs a reproduction rate** before it can be worked, or it bounces back as
  *cannot reproduce*.

## Worth keeping

- **A checkbox is a title plus a body, and only the body is complete.** Items 2, 15, 18, 20, 23 and
  28 each hid something behind their title; `BUG-42.24.5-04` was called untracked four times because
  it appears only inside item 15's body. Merged items ticked whole leave a record that cannot say
  which half was fixed — and naming that risk in a note nobody reads first does not prevent it.
- **Read every comment, not just the first.** Same error one level up: `BUG-42.24.7-01` was called
  untracked because the read stopped at `comments[0]`.
- **A note that summarises records it does not own goes stale silently.** `BUG-42.24.4-01` was
  withdrawn and `BUG-42.24.2-04` fixed on 09-08 while #2057 itself never moved. Re-read both sides.
- **`BUG-42.24.2-03` was a missing feature, not a regression** — `da0fbbcbae` is *"**feat**: add the
  token price chart"*, filed under a chain-list version it never belonged to. Most findings land
  outside the version that caught them, which is why 4–9 sit on US-42.24.19.

*Boundary: none of this is an Extension code change, requirement or release item. Canonical work
stays in [SubWallet-Mobile](https://github.com/Koniverse/SubWallet-Mobile) — same as
[notes/2026-07-30.md §A](2026-07-30.md).*
