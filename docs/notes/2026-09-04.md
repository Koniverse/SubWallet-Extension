# Notes — 2026-09-04

QC tracker for [SubWallet-Mobile #2057](https://github.com/Koniverse/SubWallet-Mobile/issues/2057)
(*Update web-runner 1.3.86*). **Updated 2026-09-07** — 8 of 12 done.

**Tick a box here when the tracker box is ticked.** Fix status is dev-side and lives here; the
tester's own verification is [US-42.24.20](../sprints/stories/US-42.24.20-qc-web-runner-verify-bugs.md)
and is not mirrored here.

## Bugs

| ✔ | Item | Bug | Sev | Fix |
| --- | --- | --- | --- | --- |
| [x] | 1. NFT ERC-721 not auto-detected on import/attach | `BUG-42.24.1-01` | P1 | no commit — likely the web-runner bump (`ddae4610e9`, `6de4df1a4d` → 1.3.89-0) |
| [x] | 2. Network toggle / disconnected badge | `BUG-42.24.2-01` + `-02` | P1 ×2 | `140b58a8fd` |
| [x] | 3. Price chart missing on token detail | `BUG-42.24.2-03` | P2 | `da0fbbcbae`, `59869ed481` |
| [x] | 4. "Migrate to Unified Account" shown twice on import (iOS) | `BUG-42.24.19-01` | P2 | no commit found |
| [ ] | 5. Create-password screen freezes on fresh install | `BUG-42.24.19-02` | **P1** | — |
| [ ] | 6. Done button layout on the Amount screen (iOS 26.6.1) | `BUG-42.24.19-03` | P2 | — |
| [ ] | 7. NFT transfer on EVM refused — message names the account, not the network | `BUG-42.24.19-04` | **P1** | — |
| [x] | 8. Substrate NFTs not displayed | `BUG-42.24.19-05` | **P1** | no commit found |
| [ ] | 9. Infinite loading after reopening the app, intermittent | `BUG-42.24.19-06` | **P1** | — |
| [x] | 10. App crashes tapping (+) when importing a custom network (Android) | `BUG-42.24.19-07` | ? | no commit found |
| [x] | 11. Wrong content on the (i) button of a locked token's other field | *not logged* | ? | no commit found |
| [x] | 12. Account name field indented out of line on Create derived account (Android) | *not logged* | ? | no commit found |

**8 of 12 done.** The four left are items 5, 6, 7 and 9 — **three of them P1**, only item 6 is P2.
Items 11 and 12 are the only two with no bug ID anywhere.

**Nothing ticked since 09-04 has a fix commit.** Items 8, 10, 11 and 12 were all ticked on 09-07 and
[PR #2058](https://github.com/Koniverse/SubWallet-Mobile/pull/2058) has been static at 33 commits
since 09-04, with no other Mobile PR or issue naming them. Observation, not a conclusion — the work
may be on an unpushed branch.

### Logged but never put on the tracker

Two bugs exist in the reports and have never appeared as a #2057 checkbox, so the dev side was never
asked about them through this channel:

| Bug | Sev | Status | |
| --- | --- | --- | --- |
| `BUG-42.24.2-04` | **P1** | `todo` | *XCM cannot fetch a fee, so no XCM transfer can be sent — **every route**.* Fee shows 0 DOT and the screen says the feature is unavailable |
| `BUG-42.24.2-05` | P2 | fixed, verified | Banxa purchase never opens the supplier |

**`BUG-42.24.2-04` is the most severe open item in the whole set** — it blocks every XCM transfer —
and it is not one of the twelve things anyone is being asked to fix.

### Correction — items 8 and 9 *were* logged

Earlier versions of this note said items 8 and 9 had "no bug ID and no report row" and that item 8
"closed with no record anywhere". **Both were wrong.** They are `BUG-42.24.19-05` and
`BUG-42.24.19-06`, logged all along.

The search looked for the tracker's wording — *"Substrate NFT"*, *"infinite loading"*, *"reopen"* —
and the report writes them as *"NFTs on substrate networks"* and *"loads forever after being left in
the background"*. No shared phrase, so the grep returned nothing and the nothing was read as absence.
**Third time this month**, after the ChainList PR and the fix commits on PR #2058: an empty search
result is a fact about the query. Fixed here by listing every logged bug title once and mapping the
twelve items against that list, rather than searching per item.

*One divergence left standing, not edited:* item 8 is ticked on the tracker while
`BUG-42.24.19-05` still reads `todo` in the report. The report is the tester's record —
[US-42.24.20](../sprints/stories/US-42.24.20-qc-web-runner-verify-bugs.md) is where a fix gets
verified, and that is their call to make.

## Open

- [ ] **`BUG-42.24.2-04` (P1) is not on the tracker** — every XCM route is unusable and nobody has
  been asked to fix it through #2057. Add it, or say why it is excluded.
- [ ] **Items 11 and 12 have no bug ID** — the only two of the twelve with no record beyond the
  tracker text.
- [ ] **Item 9 is intermittent** (*"sometimes"*) — it needs a reproduction rate before it can be
  worked, or it will bounce back as *cannot reproduce*.
- [ ] **Build identity** — both the 09-03 and 09-04 reports still read *"to fill in"*. Nine tracker
  items now hang off unnamed builds; `6de4df1a4d` puts the bundle at **1.3.89-0**. Diawi links expire.
- [ ] **Confirm item 1** was the web-runner bump and not something else.
- [x] ~~Retest story for US-42.24.1~~ — exists as
  [US-42.24.20](../sprints/stories/US-42.24.20-qc-web-runner-verify-bugs.md); AC-7/8/9 rerun and pass.
- [x] ~~Item 4 needs an ID and a home~~ — `BUG-42.24.19-01` under US-42.24.19.

## Worth keeping

- **`BUG-42.24.2-03` was a missing feature, not a regression** — `da0fbbcbae` is *"**feat**: add the
  token price chart"*. It was filed under US-42.24.2 (chain-list v0.2.122) and belonged to neither
  that version nor any other.
- **Most findings land outside the version that caught them.** `-02`, `-03` and items 4–9 are app-wide
  or first-run bugs, not chain-list ones — which is why 4–7 ended up on US-42.24.19 (regression)
  rather than on a version sub-task. A version-by-version backfill sweeps the whole app, so expect
  this to keep happening.
- **Item 2 held two bugs behind one checkbox** and was ticked without being split, so which one it
  closed was unreadable from the tracker; both were confirmed fixed by asking the developer. Split
  merged items before ticking, or the record stops being reconstructable from the record.

*Boundary: none of this is an Extension code change, requirement or release item. Canonical work
stays in [SubWallet-Mobile](https://github.com/Koniverse/SubWallet-Mobile) — same as
[notes/2026-07-30.md §A](2026-07-30.md).*
