# Notes — 2026-09-04

One note per day ([AGENTS.md](../../AGENTS.md) rule 11); one lettered section per record.

| Section | Record |
| --- | --- |
| A | The #2057 QC comment — three boxes, and what each maps to |
| B | Item 2 hides **two** bugs behind one checkbox |
| C | What the comment holds that the repo does not — build IDs and evidence |
| D | Two of the bugs may not be web-runner bugs at all |
| E | What to do — ordered, with the reason each is in that position |

---

## A. The #2057 QC comment — three boxes, four bugs

[SubWallet-Mobile #2057](https://github.com/Koniverse/SubWallet-Mobile/issues/2057) (*Update
web-runner 1.3.86*, OPEN, assigned `tunghp2002`) has **exactly one comment**, posted **2026-09-03**
by `MaiThuongNinni`. It carries two test builds and **three unticked checkboxes**.

The same session produced [report-manual.md](../tests/test-reports/2026-09/2026-09-03/report-manual.md),
which logs **four** bugs. The mapping is not one-to-one:

| # | Comment item | Report bug | Sev | Story |
| --- | --- | --- | --- | --- |
| 1 | No auto-detect NFT ERC-721 in EVM on import/attach | `BUG-42.24.1-01` | **P1** | [US-42.24.1](../sprints/stories/US-42.24.1-qc-web-runner-1-3-68.md) — `blocked` |
| 2 | "Please check the turn on/off network" | **`BUG-42.24.2-01` _and_ `-02`** | **P1** ×2 | [US-42.24.2](../sprints/stories/US-42.24.2-qc-web-runner-1-3-69.md) — `in-progress` |
| 3 | No display chart token in token details screen | `BUG-42.24.2-03` | P2 | US-42.24.2 |

Nothing is ticked, so **no item has been accepted as fixed**. All four bugs are `todo`.

---

## B. Item 2 hides two bugs behind one checkbox

This is the thing worth acting on first, because it is the one that can silently lose work.

The comment's item 2 is **titled** as one bug and **described** as a different one:

| | |
| --- | --- |
| Title | *"Please check the turn on/off network"* — this is `BUG-42.24.2-02`: **the toggle does not respond, on every network** |
| Body | *"The network never connects. The badge is the crossed-out disconnected icon even though the toggle is on"* — this is `BUG-42.24.2-01`: **Acurast connects but shows the wrong badge** |

They are different failures with different blast radii — one is a **dead control on all networks**, the
other is a **wrong status badge on one network**. A fix for either one makes the checkbox look
tickable, and **ticking it would close the other without anyone noticing**.

**Split item 2 into two checkboxes on the tracker comment.** That is a one-minute edit and it is the
only thing standing between "the toggle works again" and a permanently-lost badge bug.

### A second tension, smaller

The report says `BUG-42.24.2-01` and `-02` **"cleared on their own during the session — the networks
can be turned on again"**, and kept them logged because they are real. The comment, written the same
day, still lists them as open items to check. Both can be true — intermittent bugs are still bugs —
but whoever picks item 2 up should know it may not reproduce on first try, or they will close it as
*cannot reproduce*.

---

## C. What the comment holds that the repo does not

Three gaps, all one-way — the tracker has the evidence and the repo does not.

**1. The build identity.** The report's *Build under test* row reads literally
**"to fill in — build number + web-runner version"**. The comment has the artefacts:

| Platform | Link |
| --- | --- |
| Android | `drive.google.com/file/d/17zX_R0qpYS2BJCDRjborj3ChKlcpq3OM` |
| iOS | `diawi.com/qrcode/link/bVgYVJ` |

Without a build number, **none of the four bugs is attached to anything reproducible**. Diawi links
expire, and a Drive file can be replaced in place. This decays fastest of everything here.

**2. Evidence for the NFT bug.** `BUG-42.24.1-01`'s Screenshot column is **empty**, while the comment
carries **two videos** for it — actual and expected. The other two bugs have local images
(`img/BUG-42.24.2-01.jpg`, `img/BUG-42.24.2-03.jpg`); the P1 NFT bug has none.

**3. `BUG-42.24.2-02` has no evidence anywhere** — no local image, and the comment folds it into
item 2 without a separate attachment. It is a P1 with a written description and nothing else.

---

## D. Two of the bugs may not be web-runner bugs at all

The report says it plainly and it deserves to be surfaced rather than left in a summary paragraph:

> *BUG-42.24.2-02 and -03 happen on every network and every token, so they may not belong to this
> chain-list version at all.*

Both are logged under [US-42.24.2](../sprints/stories/US-42.24.2-qc-web-runner-1-3-69.md), whose scope
is **web-runner 1.3.69 / chain-list v0.2.122**. If a dead network toggle and a missing price chart
affect *every* network and *every* token, the likely causes are the **Mobile app shell** or something
that predates this whole backfill — not a chain-list bump that added Acurast, TRAC and vMANTA.

**This matters for where they get fixed**, not just for tidiness. Filed under a web-runner sub-task,
they will be worked as part of a 19-version backfill queue. If they are Mobile-app bugs they belong
on their own Mobile issues and can be fixed now, in parallel, by someone else.

**Cheapest test:** install the *previous* Mobile build — the one before this web-runner bump — and try
a network toggle and a token detail screen. If both fail there too, they are not this backfill's.

### Item 1 is the opposite case, and it is real parity work

`BUG-42.24.1-01` anchors on Extension
[#4568](https://github.com/Koniverse/SubWallet-Extension/issues/4568) — *Support show NFT haven't
method `tokenOfOwnerByIndex`* — which is **CLOSED, since 2025-11-25**. The Extension shows those NFTs;
Mobile does not, on the same account. That is exactly the parity gap the web-runner tracker exists to
find, and it is correctly filed.

---

## E. What to do

Ordered by what decays if left, not by severity.

1. **Record the build identity** on the report (build number + web-runner version), and mirror the two
   artefact links into it. Diawi links expire and Drive files get replaced; once that happens the four
   bugs describe a build nobody can rebuild. *Cheapest item here and the only irreversible one.*
2. **Split item 2 on the #2057 comment into two checkboxes** — `-01` (wrong badge, Acurast) and `-02`
   (toggle dead, all networks). One tick currently closes two bugs. Note on `-02` that both cleared
   mid-session, so a *cannot reproduce* is expected and is not a resolution.
3. **Run the previous-build test from §D** on `-02` and `-03`. Ten minutes, and it decides whether two
   P1/P2 bugs sit in a 19-version backfill queue or get fixed now as Mobile-app issues.
4. **Attach evidence to `BUG-42.24.1-01`** — the two videos are already in the comment; the report row
   is empty. And `-02` still has no evidence at all.
5. **Then retest `US-42.24.1` AC-7 → AC-9.** It stays `blocked` until the NFT bug is fixed, and it is
   the only story here in that state. Everything above is cheaper and unblocks or protects this.

**Not in scope of this note:** none of these is an Extension code change, an Extension requirement, or
an Extension release item. Canonical work stays in
[SubWallet-Mobile](https://github.com/Koniverse/SubWallet-Mobile) — same boundary as
[notes/2026-07-30.md §A](2026-07-30.md).
