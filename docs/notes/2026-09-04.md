# Notes — 2026-09-04

QC tracker for [SubWallet-Mobile #2057](https://github.com/Koniverse/SubWallet-Mobile/issues/2057)
(*Update web-runner 1.3.90* — retitled from 1.3.86 on 09-17). **Updated 2026-09-26 (second sync)** — **65 of 71 done**; **body: every version block ticked except 1.3.70**; **the tester's retest reads 72 fixed · 5 no-fix · 4 not fixed yet; three of the four are items the developer never ticked, and the fourth, item 70, was ticked this morning after the retest ran.**

**Tick a box here when the tracker box is ticked.** Fix status is dev-side and lives here; the
tester's own verification is [US-42.24.20](../sprints/stories/US-42.24.20-qc-web-runner-verify-bugs.md)
and is not mirrored here.

**The issue body was extended to 1.3.90 on 09-17 and fully ticked by 09-18 07:33.** 1.3.88 on
09-17; 1.3.87 and 1.3.89 at 03:40 on 09-18 — handler-only changes, no Mobile screen needed; and the
1.3.90 pair (#5072, #5064) at 07:33, **after** the screens landed. The only unticked block on the
whole body is 1.3.70 (OpenGov Phase 1, #4678), untouched since the tracker was written. #5058
passkey login and the 1.3.90 passkey offer are not listed and should not be — `extension-koni-ui`
features Mobile does not render. The tester's four sub-tasks
[US-42.24.21–24](../sprints/stories/US-42.24.24-qc-web-runner-1-3-90.md) match the four blocks and
sit in `backlog`.

**The 1.3.90 pair needed Mobile screens, and they arrived in the bump commit itself.** This note
said on 09-18 03:05 that the web-runner bundle carried only the handlers and a `signVrf` request
would land on the generic signing screen as *Signature request*. Forty minutes later
`4c4c2de57e` on [Mobile PR #2058](https://github.com/Koniverse/SubWallet-Mobile/pull/2058)
(*"feat: update webrunner 1.3.90"*, +264/−60, 29 files) bumped the bundle **and** added
`VrfSignConfirmation/index.tsx` (+86), `Detail/Substrate/Vrf.tsx` (+36), the routing in
`Confirmations/index.tsx`, `ClaimReward` and `RewardInfoPart` changes, and seven i18n keys in five
locales. Read against the Extension review: **finding 1 does not replay** — the screen shows
`new URL(request.url).origin`, not a stripped domain; **finding 2 does not replay** — the header is
`keyDerivationRequest` and the `signingRequest` case routes `isVrf` to its own screen with a comment
saying it *"must never fall through to the ordinary message-signature screen"*; and the
password-only gate (`isVrf && signMode !== PASSWORD`) is there too. The tick was placed after the
code, not before it. US-42.24.24's 22 ACs are still the tester's to run — on the PR build, since
#2058 is open, 42 commits, base `dev`, no review.

**Read every comment — there are four now.** Items 1–20 are in the
[09-03 comment](https://github.com/Koniverse/SubWallet-Mobile/issues/2057#issuecomment-5520087148),
items **21–48** in a [second one](https://github.com/Koniverse/SubWallet-Mobile/issues/2057#issuecomment-5584157750)
opened 09-08, items **49–69** in a **third** opened 09-14 03:38 UTC under *"Version test:"* — the
tester's sweep of the backlog versions, extended in place through 09-18 — and items **70–71** in a
**fourth**, opened 09-25 10:07 UTC under the same heading. Item 36 holds two sub-checkboxes of its
own; they are not items. Anything that reads `comments[0]` sees twenty items and no error — that is
how `BUG-42.24.7-01` was called untracked, and a script fixed to read two comments would now miss
two more.

## Bugs

| ✔ | Item | Bug | Sev |
| --- | --- | --- | --- |
| [x] | 1. NFT ERC-721 not auto-detected on import/attach | `BUG-42.24.1-01` | P1 |
| [x] | 2. Network toggle / disconnected badge | `BUG-42.24.2-01` + `-02` | P1 ×2 |
| [x] | 3. Price chart missing on token detail | `BUG-42.24.2-03` | P2 |
| [x] | 4. "Migrate to Unified Account" shown twice on import (iOS) | `BUG-42.24.19-01` | P2 |
| [x] | 5. Create-password screen freezes on fresh install | `BUG-42.24.19-02` | P1 |
| [ ] | 6. Done button layout on the Amount screen (iOS 26.6.1) | `BUG-42.24.19-03` | P2 |
| [x] | 7. NFT transfer on EVM refused — names the account, not the network | `BUG-42.24.19-04` | P1 |
| [x] | 8. Substrate NFTs not displayed | `BUG-42.24.19-05` | P1 |
| [ ] | 9. Infinite loading after reopening the app, intermittent | `BUG-42.24.19-06` | **P1** |
| [x] | 10. Crash tapping (+) when importing a custom network (Android) | `BUG-42.24.19-07` | P2 |
| [x] | 11. Wrong content on the (i) tooltip of the locked-balance Others row | `BUG-42.24.19-08` | P2 |
| [x] | 12. Account name indented on Create derived account (Android) | `BUG-42.24.19-09` | P2 |
| [x] | 13. Remove the Available balance field on Add proxy | `BUG-42.24.5-01` | P2 |
| [x] | 14. Call data cannot be scrolled on the Add proxy confirmation sheet | `BUG-42.24.5-02` | P2 |
| [x] | 15. Proxy type option — colour, missing dropdown, Governance offered | `BUG-42.24.5-03` + `-04` | P2 ×2 |
| [x] | 16. Double colon on the Available balance label, Remove proxy | `BUG-42.24.5-05` | P3 |
| [x] | 17. Remove proxy confirmation title truncated with space to spare | `BUG-42.24.5-06` | P3 |
| [x] | 18. Android back button dead on History **and all confirm screens** — closed **no fix**: blocked on purpose (09-17) | `BUG-42.24.19-10` | P2 |
| [x] | 19. Spacing between sections on the Unstake screen | `BUG-42.24.19-11` | P3 |
| [x] | 20. Padding and spacing on Create multisig account | `BUG-42.24.19-12` + `-13` | P3 ×2 |
| [x] | 21. Multisig confirmation screens use the old layout, not the Extension's | `BUG-42.24.7-01` | P2 |
| [x] | 22. Tab bar on the multisig account details screen cannot be scrolled sideways | `BUG-42.24.7-02` | P2 |
| [x] | 23. History UI — dead filter button, filter-row colour, two wrong title colours | `-03` `-04` `-05` `-07` | P3 ×4 |
| [x] | 24. Pending record in the Multisig tab has no date heading | `BUG-42.24.7-06` | P2 |
| [x] | 25. Notification screen UI — Mark all as read overruns the right edge | `BUG-42.24.7-08` | P2 |
| [x] | 26. Navigation on tapping a notification — **multisig only**; claim unlogged | `BUG-42.24.7-09` | P2 |
| [x] | 27. Swap button is not disabled on a multisig account | `BUG-42.24.7-10` | P2 |
| [x] | 28. MANTA earning offered on multisig; **DOT single-option screen unlogged** | `BUG-42.24.7-11` | P2 |
| [x] | 29. Notification settings has no Pending multisig approvals option | `BUG-42.24.7-12` | P2 |
| [x] | 30. Multisig accounts have no multisig badge on their avatar | `BUG-42.24.7-13` | P2 |
| [x] | 31. Select account screen for a new multisig shows no account-type badges | `BUG-42.24.7-14` | P2 |
| [x] | 32. The account filter on Export account has no Multisig option | `BUG-42.24.7-15` | P2 |
| [x] | 33. Toast on a pending multisig transaction hidden behind the detail sheet — *the "spacing varies by device height" follow-up note was removed 09-16* | `BUG-42.24.7-16` | P2 |
| [x] | 34. Multisig tab does not offer to enable a network that is turned off | `BUG-42.24.7-17` | P2 |
| [ ] | 35. View transaction does not open the transaction details — **"Can not reproduce, may be network unstable, we will follow up"** (09-16) | `BUG-42.24.7-18` | P2 |
| [x] | 36. Governance pending-multisig — **code check**; ticked with the *"Recheck Submit/Approve/Reject"* sub-box still open | *none — not a bug* | — |
| [x] | 37. Cross-chain transfer from a multisig refused with the wrong message | `BUG-42.24.7-19` | P2 |
| [x] | 38. Signatory validation message shown as a red block, not a tooltip — **ticked as declined: "requires a different mechanism on mobile, keep as is"** | `BUG-42.24.7-20` | P3 |
| [x] | 39. Multisig transaction detail sheet does not match the Extension | `BUG-42.24.7-21` | P2 |
| [x] | 40. Select an account to sign sometimes never finishes loading (dApp via multisig) | `BUG-42.24.7-22` | **P1** |
| [x] | 41. Pending Multisig popup — Sender/Recipient columns misaligned | `BUG-42.24.7-23` | P2 |
| [ ] | 42. Scrolling performance through Pending Multisig records — **deferred: "optimization scope still unclear, needs investigation"** | *none logged* | — |
| [x] | 43. Insufficient balance warning on Add proxy confirmation not styled like the Extension | `BUG-42.24.7-24` | P3 |
| [x] | 44. Creating a multisig that already exists as a watch-only account shows a raw error key | `BUG-42.24.7-25` | P2 |
| [x] | 45. Starting a second identical multisig transaction shows a raw error key | `BUG-42.24.7-26` | P2 |
| [x] | 46. "Unable to sign transaction" shown while the signatory list is still loading | `BUG-42.24.7-27` | P2 |
| [x] | 47. Signatory picker shown on a network that does not support multisig | `BUG-42.24.7-28` | P2 |
| [x] | 48. Subnet token cannot be transferred — form has no validator fields and no subnet name | `BUG-42.24.9-01` | **P1** |
| [x] | 49. Insufficient-balance popup on Start earning uses the system dialog, not the app's (1.3.78, #4803) | `BUG-42.24.11-01` | P2 |
| [x] | 50. Manage networks does not match the Extension after Disable all networks (1.3.78, #4970) | `BUG-42.24.11-02` | P3 |
| [x] | 51. Align the Available section and add the missing notification on Alpha / TAO transfer (1.3.78, #4900) | `BUG-42.24.11-03` | P3 |
| [x] | 52. Swap screen — `simple id: undefined` on confirmation **fixed**; token-name truncation **declined**: "may affect other devices and tokens" (1.3.78, #4899) | `-11-05` fixed · `-11-04` declined | P2 · P3 |
| [x] | 53. *Go to parent* button on a nested NFT has no button styling (1.3.80, #4884) | `BUG-42.24.13-01` | P3 |
| [x] | 54. NFT information block differs from the Extension — empty panel + stray Resources row, and fields laid out differently; Normal and Nested NFT (1.3.80, #4884) | `BUG-42.24.13-02` + `-03` | P3 ×2 |
| [x] | 55. Info icon in the Description popup stretched down the height of the text — **the (i) icon's bar stretches the full text height with the icon pinned to the top; Extension centres a small icon on a short bar** (1.3.80) | `BUG-42.24.13-05` | P3 |
| [x] | 56. Unstake confirmation drops the TAO unstaking-fee notice — **ticked as push-back: "Recheck, native staking have no notice"**; the tester logged it under 1.3.84 anyway and marks it *fixed* | `BUG-42.24.16-01` | P2 |
| [x] | 57. *"Please check the issue in [#2053](https://github.com/Koniverse/SubWallet-Mobile/issues/2053)"* — a **second tracker** (react-native 0.82.1, May 2026) with **fourteen** open items — **ticked 09-16 while #2053 is still 0 of 14** | *none logged* | — |
| [x] | 58. No error shown on an invalid QR — address book, import token/NFT, WalletConnect, scan-address, attach account, import by QR (= #2053 item 3) | `BUG-42.24.19-15` | P2 |
| [x] | 59. **App crashes** on the Multisig tab when tapping the History tab — filed and ticked in one edit | `BUG-42.24.19-16` | **P1** |
| [x] | 60. Call data row on the Signature request screen has no info button when signing for a dApp through a multisig — filed and ticked in one edit | `BUG-42.24.7-30` | P2 |
| [x] | 61. Missing right padding on the *NFT structure* button, nested NFT (1.3.80, #4884) — filed and ticked in one edit | `BUG-42.24.13-06`? (tester logs *cards have no padding above/below* — same screen, different edge) | P3 |
| [x] | 62. NFT information block differs from the Extension, **nested** NFT (1.3.80, #4884) — the nested half of item 54, filed and ticked in one edit | *none logged* — `-13-03` may cover it | — |
| [x] | 63. **WalletConnect offers no account** on an EVM network the wallet already has — and the WC popup cannot be dismissed after reopening the app — ticked whole 09-18 04:24 | `BUG-42.24.19-17` + `-18` | **P1 ×2** |
| [x] | 64. Spinner on every character typed into the amount field — change validator, subnet staking | `BUG-42.24.19-14`? (= #2053 §2, same symptom) | P2 |
| [x] | 65. Screen occasionally freezes when uploading an invalid image in Scan | *none logged* | — |
| [x] | 66. dApp logos in the Recent row are different sizes | *none logged* | — |
| [x] | 67. Method and Info rows on Transaction details cannot be collapsed | *none logged* | — |
| [x] | 68. **Unclaimed rewards read 0 TAO on Bittensor native staking** — nothing can be claimed — ticked 09-24; **`-24-01` verified fixed the same day, US-42.24.24 closed `done`** | `BUG-42.24.24-01` | P2 |
| [ ] | 69. Available balance slow to load on the claim-rewards select-account screen, all-accounts mode — **deferred "hard to reproduce"; the tester closed `-24-02` *no fix* independently — it did not reproduce on a second device** | `BUG-42.24.24-02` | P3 |
| [x] | 70. Recipient address field on the Transfer screen is out of line (iOS) — filed 09-25 **with its row already logged**, ticked **09-26 04:42 — one day** | `BUG-42.24.19-23` | P3 |
| [ ] | 71. **"Pay attention!" popup on Claim rewards confirm does not match the Extension** (iOS) — filed 09-25 **with its row already logged**; third finding in a row on the claim-rewards flow after 68 and 69 | `BUG-42.24.19-24` | P2 |

Open: **6, 9, 35, 42, 69, 71** — six. 35 is verified fixed and can simply be ticked; 42 and 69 are deferred with a stated reason; 71 was filed 09-25; 6 and 9 have been open since 09-03.

**The two sides agree for the first time since 09-03.** US-42.24.20's retest now reads
**72 fixed · 5 no-fix · 4 not fixed yet** across 81 rows, against 54 · 2 · 15 across 71 on 09-16.
**The eleven-row table that used to sit here — ticked by the developer, unverified by the tester —
is empty.** All eleven were rerun and passed, including `BUG-42.24.19-16`, the **P1 crash** on
History (item 59) that the tester could still reproduce on 09-16, and `-19-15` (item 58), `-7-09`
(26), `-7-16` (33), `-7-28` (47), `-7-30` (60), `-11-02` (50), `-13-03` (54) and `-13-06` (61). The
four rows still reading *not fixed yet* are 6 (`-19-03`), 9 (`-19-06`, **P1**), 70 (`-19-23`) and
71 (`-19-24`) — and until this morning all four were items the developer had never ticked.

**Item 70 was ticked 09-26 04:42, so one row is now out of step again — but it is lag, not a
dispute.** The retest that recorded `-19-23` as *not fixed yet* ran on 09-25, **before** the tick.
That is the shape item 58 had: ticked after the retest, then verified on the next pass. The thing
to watch is whether it clears on the next run. What ended on 09-24 and has not returned is the
other shape — **a tick the tester reran and could not confirm**; there are still none of those.

**Item 68 is settled, and it took six days.** `BUG-42.24.24-01` — unclaimed rewards reading
**0 TAO** on every Bittensor native position while the same accounts on the Extension read 497.71
and 0.0068 — was verified fixed **2026-09-24**, the same day the developer ticked it. US-42.24.24
closed **`done` at 21 of 22, 1 skipped**; AC-10 reran on the fixed build and passes, so the eight
ACs stopped behind it all ran. **No story is `blocked` any more** — a status value that had one
user in 336 now has none. The shape is still worth keeping: the manual-claim capability
[US-12.23](../sprints/stories/US-12.23-bittensor-manual-claim-native-staking.md) shipped in 1.3.90
worked on one surface and returned nothing on the other for six days, and **US-12.23's AC-3 had
already named that failure mode** — `I96F32` fixed-point decode, *a plausible wrong number*, and
zero is a plausible wrong number — with no test behind it, nor behind AC-4's `0` threshold
fallback. Whose bug it was, Extension or Mobile, was never recorded.

**Item 69 was deferred twice, independently, and that is why it holds.** The developer left it
unticked 09-24 as *hard to reproduce*; the tester logged `BUG-42.24.24-02` and closed it **no fix**
on a reason of their own — the slow load did not reproduce on a second device, so it reads as the
test phone rather than the build. The recheck this note asked for (*if 68's fix changed how that
screen loads, 69 may already be gone*) is moot: the answer came from a second device, not a second
build. Fifth deferral-with-a-reason, and the first one corroborated from the other side.

**Items 64–67 all have rows now, and all four passed.** `-19-19` (spinner per keystroke on the
change-validator amount field), `-19-20` (Scan freezes on an invalid image), `-19-21` (dApp logo
sizes) and `-19-22` (Method / Info rows cannot be collapsed) were logged round 2 and verified. The
09-18 sweep that was *ticked against nothing* is backed after the fact. **Item 64 was not a
partial** — `-19-14` covers the account-name and amount fields and reads fixed, and the
change-validator field got `-19-19` of its own, also fixed. **Item 63's whole-tick was right too**:
`-19-17` and `-19-18` both pass, so the single tick over two P1s turned out honest. Both were open
questions in this note; both closed without anyone answering them — the retest did.

**Items 70 and 71 arrived the other way round.** Filed 09-25 with `BUG-42.24.19-23` and `-24`
logged the **same day** — the first items on this tracker to come with their rows attached rather
than be ticked against nothing. **70 was ticked the next morning, 09-26 04:42 — a one-day
turnaround, the fastest on this tracker.**

**71 is the one left, and it is the more serious of the pair.** 70 was a P3 alignment on the
Transfer screen; 71 is **P2**, the *"Pay attention!"* popup on the **Claim rewards confirm** screen
not matching the Extension. Same comment, same day, same platform — the P3 went first. And 71 is
the **third** finding in a row on the claim-rewards flow, after 68 (the release blocker) and 69:
one screen, three findings, worth looking at as a screen rather than as three tickets.

**The regression is running again, and the checklist grew while it ran.** US-42.24.19 round 2:
77 → **82 lines**, after the tester read the SubWallet user guide against the checklist and found
four documented features with no line of their own — Trust Wallet import, bridge transfer,
parachain (collator) staking, change validator — plus a split of REG-70, which had carried account
migration and the Subscan API key on one line. **Android 37 of 82, iOS 21 of 82**; 58 of 169 lines
across both platforms. Governance is left out on purpose — Mobile has no governance screen, which
is the same reason `-5-04` is closed *no fix*.

**Item 18 is closed *no fix*, not fixed.** `BUG-42.24.19-10` — *the developer is blocking the back
button on History on purpose*. The tick on 18 was read as a fix for a week; it was a decision.
Third no-fix row after `-5-04` and `-7-20`, now five with `-11-04` and `-24-02`.

**What the open items are.** 6 (`-19-03`, Done-button layout on iOS 26.6.1, P2) and 9 (`-19-06`,
intermittent infinite loading, **P1**) have been open since the tracker was written on 09-03 —
**23 days**, while every other item filed since has been ticked within a day or two. 35 (`-7-18`) is
*cannot reproduce* on the developer's side and **fixed** on the tester's; it can be ticked. 42 has
no bug row and is deferred pending investigation. 69, 70 and 71 are covered above.

**How the ticks were made.** Six merged checkboxes hid two or more bugs behind one title (2, 15, 18,
20, 23, 54) and were ticked whole, so the tick does not say which half; 52 is the one exception —
ticked with a line saying `-11-05` fixed and `-11-04` declined. Three ticks are decisions, not
fixes: 38 (`-7-20`, no tooltip on mobile), 52's `-11-04` half, and 56 (disputed — *native staking
has no notice* — the tester logged it anyway and verified it fixed, so the push-back did not hold).
One tick, 36, sits over an unticked sub-box (the Governance recheck). Five items were filed and
ticked in the same edit (56, 59, 60, 61, 62). 57 ticks a **pointer to #2053**, which is **still 0
ticked** on its own page; the tester is pulling its items into US-42.24.19 one at a time
(§2 → `-19-14`, §3 → `-19-15` = item 58, §6 → `-19-01`).

**The tracker spans four comments and two programmes.** 1–20 (09-03) are the first web-runner
sweep; 21–48 (09-08) are the multisig round plus regression; 49–69 (09-14, extended through 09-18)
sweep the versions that were still in backlog — 1.3.78, 1.3.80 — and then the round-2 findings;
70–71 (09-25) are round 2 of the regression. Items 2, 15, 18, 22, 26, 28, 36, 52, 54 all had bodies
wider than their titles; read the body.

**Two bugs the tester logged that are on no checkbox at all — both now fixed.** `BUG-42.24.7-29`
(Approval required notifications stay after the initiator rejects) and `BUG-42.24.13-04` (NFT
transfer confirmation does not match the Extension). Both fixed and verified without ever appearing
on #2057, the `-2-04`/`-2-05` shape again: **the tracker is not the full bug list, and a fix can
land without any box moving.**

**The programme itself moved.** 23 sub-tasks exist — numbered .1 to .24, **with .3 absent** —
plus the parent. **20 done (166 pts), 1 closed unrun (8, 1.3.85 — none of its 11 AC could be tested
on Mobile), 2 in progress (28: the regression and the verify story).** Nothing in `backlog`,
nothing `blocked`.

## Open

- [ ] **Item 71 is the only new one left** — **P2**, the *Pay attention!* popup on Claim rewards
  confirm. Its P3 neighbour, 70, was filed and ticked inside a day; 71 was filed the same day and
  is still open.
- [ ] **`-19-23` needs one rerun to close** — item 70 was ticked 09-26, after the 09-25 retest
  recorded it *not fixed yet*. Almost certainly lag, like item 58 was; it just needs the next pass
  to say so, or it becomes the first unconfirmed tick since 09-24.
- [ ] **The claim-rewards flow has taken three findings in a row** — 68 (the 1.3.90 blocker, fixed),
  69 (slow Available balance, deferred both sides) and 71 (the *Pay attention!* popup, open). Look
  at the screen once, rather than at the next ticket on it.
- [ ] **Item 9 needs a reproduction rate** before it can be worked, or it bounces back as *cannot
  reproduce*. **Open 23 days**; the only P1 the developer has not ticked, and one of the four rows
  the tester cannot verify.
- [ ] **Item 6 is the other one** — Done-button layout, iOS 26.6.1, open since 09-03. 6 and 9 have
  now outlived every bug filed after them.
- [ ] **Item 35 can be ticked** — `-7-18` verified fixed despite the *cannot reproduce* note.
  Unchanged since 09-16; one click, one row closed.
- [ ] **Item 36's Governance recheck is still open under a ticked parent** — Submit / Approve /
  Reject on a pending Governance multisig, on a testnet that submits. The parent tick was the spec
  read, not this. Mobile has no governance screen, which is why `-5-04` is *no fix* — so decide
  whether this sub-box can ever be ticked, or strike it.
- [ ] **Item 42 is deferred with no target** — device, record count, jank measure, or it cannot be
  ticked honestly. The only one of the five deferrals with no reason attached.
- [ ] **`BUG-42.24.2-04`'s status still contradicts itself** — **fixed** and ✅ pass in
  US-42.24.20, but `todo` in US-42.24.2 with **AC-10 still unticked**. Rerun AC-10 or close the row.
  *Corrected 2026-09-26: this note also named US-42.24.19 as carrying a `todo` copy — it does not
  mention `-2-04` at all. Two files, not three.*
- [ ] **Items 26 and 28 are ticked and half of each is unlogged** — the claim-notification video in
  26, the DOT single-option screen in 28. Both logged halves now pass, so the unlogged halves are
  the only part still claimed against nothing.
- [ ] **Item 18's confirm screens and item 21's other multisig screens** — ticks wider than the
  records. `-7-01` now passes, but on the Transfer screen only; `-19-10` is closed *no fix*, so
  item 18's tick covers a decision on History and nothing at all on the confirm screens.
- [ ] **Item 57 / #2053** — the tester is pulling #2053 into US-42.24.19 (§2 → `-19-14`,
  §3 → `-19-15`, §6 → `-19-01`). **#2053 is still 0 ticked on its own page** while its items are
  fixed here. Close or cross-reference it once the pull is done.
- [ ] **Two bugs were fixed without ever reaching a checkbox** — `-7-29` and `-13-04`. No harm this
  time, but it means #2057 is the dev-side tick list, not the bug list. US-42.24.20 is the bug list.
- [ ] **Build identity** — the 09-03, 09-04 and 09-08 reports read *"to fill in"*, and **so does
  09-25**. Four reports, no build number: either fill the field or drop it from the template.

## Worth keeping

- **A checkbox is a title plus a body, and only the body is complete.** Items 2, 15, 18, 20, 23 and
  28 each hid something behind their title; `BUG-42.24.5-04` was called untracked four times because
  it appears only inside item 15's body. Merged items ticked whole leave a record that cannot say
  which half was fixed — and naming that risk in a note nobody reads first does not prevent it.
- **Read every comment, not just the first.** Same error one level up: `BUG-42.24.7-01` was called
  untracked because the read stopped at `comments[0]`.
- **A note that summarises records it does not own goes stale silently.** `BUG-42.24.4-01` was
  withdrawn and `BUG-42.24.2-04` fixed on 09-08 while #2057 itself never moved. Re-read both sides.
- **`BUG-42.24.2-03` was a missing feature, not a regression** — `da0fbbcbae` is *"**feat**: add the
  token price chart"*, filed under a chain-list version it never belonged to. Most findings land
  outside the version that caught them, which is why 4–9 sit on US-42.24.19.

*Boundary: none of this is an Extension code change, requirement or release item. Canonical work
stays in [SubWallet-Mobile](https://github.com/Koniverse/SubWallet-Mobile) — same as
[notes/2026-07-30.md §A](2026-07-30.md).*
