# Notes — 2026-07-23

Working notes for the maintenance-ledger consolidation. One file per day
([AGENTS.md](../../AGENTS.md) rule 11); one lettered section per ledger folded.

---

## A. EPIC-36 (Maintenance — Hardware Wallet) merged into EPIC-16

**81 ledger stories dissolved; 90 rows across 10 stories.** EPIC-36 held one story per tracker
issue for the hardware-wallet area. It is deleted; its issues are now incremental-work rows on the
capability they belong to.

### Routing

| Destination | Rows | What it holds |
|---|---|---|
| [US-16.1](../sprints/stories/US-16.1-ledger-hardware-wallet-signing.md) | 12 | the signature itself — first delivery, EVM app, pre-sign validation |
| [US-16.2](../sprints/stories/US-16.2-qr-signing-keystone-and-polkadot-vault.md) | 3 | Keystone, all three |
| [US-16.3](../sprints/stories/US-16.3-additional-hardware-wallets.md) | 4 | Trezor, Tangem, D'Cent, Keystone 3 Pro — all open |
| [US-16.4](../sprints/stories/US-16.4-ledger-network-and-app-coverage.md) **(new)** | 19 | one more chain, one more chain-specific app |
| [US-16.5](../sprints/stories/US-16.5-ledger-connection-and-account-discovery.md) **(new)** | 13 | plugged-in device → usable account |
| [US-16.6](../sprints/stories/US-16.6-ledger-signing-across-wallet-features.md) **(new)** | 14 | the hardware branch each feature needed |
| [US-16.7](../sprints/stories/US-16.7-generic-ledger-app-migration-and-metadata.md) **(new)** | 14 | Zondax Generic app + five metadata rounds |
| [US-16.8](../sprints/stories/US-16.8-ledger-asset-recovery-tool.md) **(new)** | 2 | the offline recovery tool, out-of-repo |
| [US-16.9](../sprints/stories/US-16.9-ledger-connection-and-derivation-improvements.md) **(new)** | 5 | open, extension |
| [US-16.10](../sprints/stories/US-16.10-ledger-on-webapp-and-avail-space.md) **(new)** | 4 | open, WebApp & Avail Space |
| [EPIC-16](../sprints/epics/EPIC-16.md) | 2 umbrellas | #4408, #4175 — rule 10 |

Arithmetic: 81 ledger issues − 1 umbrella (#4408) + 5 settled recoveries + 4 open recoveries
+ 1 issue the ledger had merged into another's row (#3402) = **90 rows**.

### The shape of the area

**78 of 81 issues are Ledger; 3 are Keystone; Polkadot Vault has none.** The QR transport has no
cable to disconnect, no firmware to fall behind, and no per-chain app to keep current. The USB one
has all three — and one Ledger-specific programme, the Generic app, accounts for 14 rows on its own.

**Vara is the whole argument for the Generic app in two rows.** #2283 *"Support Ledger for the Vara
network"* closed `NOT_PLANNED`; two years later #3835 shipped Vara in 1.3.9 **through the Generic
app**. The per-chain route was abandoned; the platform route delivered.

**Five rounds of "update the runtime metadata for the Ledger app"** (#4480, #4653, #4715, #4828,
#4876) and **not one carries a CHANGELOG line.** A chain upgrades its runtime, the Generic app's
metadata goes stale, the device can no longer render what it is asked to sign. The work ships
inside releases without being named, which is why the ledger showed every round as `—`.

### Two rule-12 corrections, one each direction

Same field, opposite errors, in the same ledger:

| Issue | Tracker | Board | Ledger said | Now |
|---|---|---|---|---|
| [#4225](https://github.com/Koniverse/SubWallet-Extension/issues/4225) | CLOSED / COMPLETED | **Cancel** | ✅ done | ⏸ deprecated |
| [#3173](https://github.com/Koniverse/SubWallet-Extension/issues/3173) | CLOSED / COMPLETED | **Done** | ⏸ deprecated | ✅ done |

A third case is neither: **#4980** is closed `COMPLETED` while its board column still reads
`Ready to build`. That is a stale column, not a decision — the two are distinguishable, and the
distinction matters: `Cancel` on a closed issue overrides the tracker, `Ready to build` on a closed
issue is simply a card nobody moved.

### A CHANGELOG typo created a phantom row and hid a real release

The ledger's US-36.61 read:

> `#3902 — "Optimize swap pair selection" — 📋 backlog`

**#3902 is not that issue.** Its tracker title is *"[WebApp] Check the `@polkadot/api` library
version for signing with Ledger"* — a real, open, Ledger issue. The title came from
[docs/CHANGELOG.md:881](../CHANGELOG.md), which delivers *"Optimize swap pair selection"* in 1.3.30
and **mis-numbers it `(#3902)` when the issue is [#2902](https://github.com/Koniverse/SubWallet-Extension/issues/2902)**.

The generator trusted the bullet. Two consequences, one in each direction:

1. A Ledger issue was filed in the hardware ledger under a swap issue's name — **fixed here**, the
   row is in [US-16.10](../sprints/stories/US-16.10-ledger-on-webapp-and-avail-space.md) with the
   tracker's title.
2. `US-31.18`, which correctly owns **#2902**, shows `Shipped: —` — because the version matcher
   looked for `(#2902)` and the line says `(#3902)`. **The release exists: 1.3.30.** Not fixed here;
   US-31.18 belongs to the swap ledger, which is still unfolded. **Flag for the EPIC-31 fold.**

A one-digit typo in a changelog bullet is enough to both invent a row and erase a version. Neither
failure is visible from inside the generated docs — only the tracker disagrees.

### #2453 was named in prose for a year and owned by nobody

[#4408](https://github.com/Koniverse/SubWallet-Extension/issues/4408) *"Integrate Polkadot Ledger
App"* has five children and no CHANGELOG line — an umbrella by rule 10. Its child list is the only
place [#2453](https://github.com/Koniverse/SubWallet-Extension/issues/2453) was reachable from:
*"Integrate Polkadot Ledger app from Zondax"*, shipped in 1.2.11, carrying its own CHANGELOG line,
cited in [US-16.1](../sprints/stories/US-16.1-ledger-hardware-wallet-signing.md)'s implementation
notes as evidence — and held by **no story's table**. The same list separates #3402, which the
ledger had merged into #3307's row.

### Scope that never reached a table

Nine issues named in EPIC-16's own prose were owned by no row:

- **#43, #150, #327, #538** — US-16.1's implementation notes cite them as the evidence for its 0.5.4
  anchor. Now rows. They also give the story its real spine: #43 (0.3.1) shipped *attach, show
  balance, receive* — a Ledger account you could look at but not spend from; #327 (0.5.2) is that
  gap reaching users as a signing popup that could neither complete nor close; #150 (0.5.4) is the
  delivery. The capability took two releases of *almost*.
- **#1387, #1829, #2273, #4128** — US-16.3's four devices, named in its Background, its acceptance
  criteria and its References, in no table. Now rows.
- **#2453** — above.

### #4175's children are in three different ledgers

| Child | Title | Filed in |
|---|---|---|
| #1387 | Integrate with Trezor | US-16.3 ✅ |
| #1829 | Integrate Tangem Cold Wallet | US-16.3 ✅ |
| #831 | Integrate with AirGap Vault for QR signer | **dApp ledger (EPIC-30)** |
| #3857 | Unable to send funds out of Polkadot Vault | **Transactions ledger (EPIC-28)** |

Both misplaced children are hardware work: AirGap Vault extends US-16.2's QR path, #3857 is a
Polkadot Vault defect. **Not moved** — EPIC-30 and EPIC-28 are unfolded and reaching into them would
leave their counts wrong. Recorded in [EPIC-16](../sprints/epics/EPIC-16.md); route them when those
ledgers fold.

Note also that #4128 (D'Cent) and #2273 (Keystone 3 Pro) are named by US-16.3 but are **not**
children of #4175. Neither list is the whole roadmap.

### One version recovered, two rejected

Of 42 rows showing `Shipped: —`, the linked-PR path
([AGENTS.md](../../AGENTS.md) rule 1b-bis) recovered exactly one:

- **#2782 → 1.1.55.** No CHANGELOG line, no commit naming it — the developer titled the PR
  [`[Issue 2608]`](https://github.com/Koniverse/SubWallet-Extension/pull/2762) while its body
  delivers #2782. GitHub's `ConnectedEvent` still links it; the merge commit is first contained in
  `v1.1.55`.

Two candidates were **rejected as version-space collisions** (rule 1b) — the commit-grep path found
`(#1096)` and `(#1571)` in *polkadot-js* commits, "Bump deps" by Jaco (2022-06-25) and "Update
polkadot-js deps" by Tarik Gul (2025-07-04). Neither `v0.44.2` nor `v0.61.2` is a `(Koni)` release.
**The grep path needs the lineage check every time**; the linked-PR path does not, because a
ConnectedEvent cannot cross repositories.

### Rule 9 splits

Two, both by axis rather than by convenience:

- **US-16.5 / US-16.9** — connect-and-discover: 13 settled rows stay, 5 open ones leave.
- **US-16.7 / US-16.9** — the Generic-app migration: 14 settled rows stay, #3392 (auto-migrate
  accounts off the old apps) leaves. It is the unfinished quarter of that programme — the app was
  integrated, the WebApp followed, manual attachment shipped; doing it *for* the user never did.

**US-16.9 and US-16.10 are then split from each other by version space**, not by feature: US-16.9's
five ship in an extension release, US-16.10's four in the WebApp / Avail Space space, which has its
own numbering (rule 1b). Rows that can never share a `Shipped` value do not belong in one table.

### US-16.8 is a rule-4 third-branch story

*"Implement an offline tool to recover assets from Ledger"* (#2200) and its UI follow-up (#2521) are
closed `COMPLETED` with board `Done`, and **all three evidence paths return nothing** — no CHANGELOG
line, no commit, no merged PR. That is the correct result: the issue body points at Talisman's
`ledger-substrate-recovery` as prior art, and the tool ships outside this repository. `done` with
`version_shipped` and `commit` deliberately empty ([AGENTS.md](../../AGENTS.md) rule 4, third
branch).
---

## B. EPIC-31 (Maintenance — Swap) merged into EPIC-11

**108 ledger stories dissolved; 106 rows across 17 stories.** The swap ledger held one story per
tracker issue. It is deleted; its issues are now incremental-work rows on the capability they
belong to.

Arithmetic: 108 ledger issues − 8 umbrellas + 3 issues the ledger merged into other rows or missed
(#3651, #4090, #4114) + 3 named in EPIC-11 prose and held by no table (#4024, #1793, #2920)
= **106 rows**.

### Six new stories, because the FR map is provider-shaped and the maintenance is not

EPIC-11's eleven stories are eleven providers: FR-103 … FR-113, one requirement each. But **half the
ledger is not about a provider** — it is about the swap screen, the quote pipeline, the pair
catalogue, the fee. No FR says "the swap screen", so those issues had no capability to belong to,
and filing each under whichever provider happened to be selected at the time would have been a
fiction.

| New story | Rows | |
|---|---|---|
| [US-11.12](../sprints/stories/US-11.12-swap-ui-content-and-confirmation.md) | 17 | UI, content, ToS, confirmation |
| [US-11.13](../sprints/stories/US-11.13-swap-quote-pipeline-and-service.md) | 10 | quote pipeline & service internals |
| [US-11.14](../sprints/stories/US-11.14-swap-pair-catalogue-and-tooling.md) | 4 | CMS/backend catalogue, out-of-repo |
| [US-11.15](../sprints/stories/US-11.15-providers-evaluated-not-integrated.md) | 5 | five decisions not to build |
| [US-11.16](../sprints/stories/US-11.16-swap-flow-improvements.md) | 10 | open, provider-neutral |
| [US-11.17](../sprints/stories/US-11.17-swap-coverage-expansion.md) | 5 | open, per-provider reach |

### The flagged fix: #2902 → 1.3.30

The [EPIC-36 fold](2026-07-23.md#a-epic-36-maintenance--hardware-wallet-merged-into-epic-16) found
that [docs/CHANGELOG.md](../CHANGELOG.md) line 881 — *"Optimize swap pair selection (#3902)"* —
**mis-numbers #2902 as #3902**, and flagged that the swap ledger's US-31.18 therefore showed
`Shipped: —` for work that shipped in **1.3.30**. That is now fixed: #2902 is a row in
[US-11.13](../sprints/stories/US-11.13-swap-quote-pipeline-and-service.md) with its release.

**No CHANGELOG bullet anywhere in the file contains the string `(#2902)`** — the only line that
delivers it is the one that names the wrong issue. A version matcher keyed on the issue number can
never find it; only a reader comparing the bullet text to the tracker can.

### Two more mis-numbered bullets, found by looking

Once the pattern was known it was worth checking the rest of the area. Two more:

| Bullet | Says | Means | Consequence |
|---|---|---|---|
| `Add validate for Solochain … (#3464)` @1.2.25 | issue #3464 | **PR** #3464, for issue #3463 | coverage row attributed to a PR number; the version it claims (1.2.25) is contradicted by `git tag --contains`, which puts the merge in **v1.2.26** |
| `Update chain-list stable v0.2.112 (#4517)` @1.3.54 | issue #4517 | a **partial** delivery of #4517, which is still open | the ledger took the bullet as #4517's title *and* as proof it shipped; #4517 is *"Add support for 1-click swap between native BTC & tBTC on Hydration"* and is `Follow Up` on the board |

Three mis-numbered bullets in two areas is not a typo class — it is the cost of a changelog whose
issue references are hand-written and never checked against the tracker.

### Four rule-12 corrections, two each direction

| Issue | Tracker | Board | Ledger said | Now |
|---|---|---|---|---|
| [#4105](https://github.com/Koniverse/SubWallet-Extension/issues/4105) 1inch | CLOSED / COMPLETED | **Cancel** | ✅ done | ⏸ deprecated |
| [#4306](https://github.com/Koniverse/SubWallet-Extension/issues/4306) SimpleSwap pairs | CLOSED / COMPLETED | **Cancel** | ✅ done | ⏸ deprecated |
| [#2925](https://github.com/Koniverse/SubWallet-Extension/issues/2925) no swap quote | CLOSED / COMPLETED | Done | ⏸ deprecated | ✅ done |
| [#3274](https://github.com/Koniverse/SubWallet-Extension/issues/3274) Chainflip version | CLOSED / COMPLETED | Done | ⏸ deprecated | ✅ done |

#4105 is the sharpest: it is the direct sibling of #4144 (KyberSwap) under the umbrella #4155 — two
EVM aggregators opened together, one shipped in 1.3.36, one cancelled. Reading the cancelled one as
`done` would put a handler in the docs that does not exist in the code.

### A child that is a PR is not a child

Rule 10 asks whether an issue has children. In this area GitHub's sub-issue lists say yes eleven
times when the answer is no:

> #75, #79, #87, #89, #101, #123, #132, #151, #154, #591, #599 — linked as children of #4389, #4495,
> #4496, #4573, #4724, #4733 and #4791. **Four are pull requests** (`Update yarn.lock`, `Fix UI in
> General Settings, Auth Management`), the rest are unrelated 2022 issues.

Without filtering them first, **#4724 — a Chainflip SDK bump — would have been classified as an
umbrella** on the strength of two bogus links, and its real work would have vanished from every
table. The filter has to run *before* rule 10, not after. This is the second area where the pattern
has appeared; the first was the hardware ledger's ten.

After filtering, eight real umbrellas remain, and two candidates stay rows: **#4724** (no children
at all) and **#4733** (one real child, #4724, which is an incidental sub-task rather than a phase).

### An umbrella's area is the area of its children

[#4096](https://github.com/Koniverse/SubWallet-Extension/issues/4096) — *"Integrate Bitcoin into
SubWallet and support cross-chain swap"* — was filed in the swap ledger and has **12 children, only
2 of which are swap**. The rest are Bitcoin account, transfer, dApp-connect and UTXO-indexing work,
already owned by stories in EPIC-4, EPIC-7, EPIC-9, EPIC-24, EPIC-28, EPIC-30 and EPIC-41. Its one
genuinely unowned child, [#4434](https://github.com/Koniverse/SubWallet-Extension/issues/4434)
*"Optimize request handling in the SendFund form"*, is transaction work — **flag for the EPIC-28
fold.**

### Scope that never reached a table

Five issues named in EPIC-11's own prose were owned by no row:

- **#4090, #4114** — quoted at length in
  [US-11.8](../sprints/stories/US-11.8-cross-chain-swap-routing.md)'s Implementation notes as the
  proof of its 1.3.30 anchor (round 1 delivered BRIDGE→SWAP only; round 2 added SWAP→BRIDGE). Now
  rows.
- **#4024** — US-11.10's only tracker issue (PiperX). Now a row.
- **#1793, #2920** — US-11.11's two (StellaSwap). Now rows. #1793 predates the swap feature's first
  delivery in 1.1.55 entirely: it is a dApp-era question about a DEX still not integrated.

Plus **#3651** *"Add swap pairs on Hydration"*, which the ledger had merged into #3633's row — the
same shape as #3402 in the hardware fold.

### What the area's shape says

**Zero versions were recovered from 57 rows showing `Shipped: —`**, and that is the finding rather
than a failure. Two candidates surfaced and both were rejected: PR #4589, linked to #4517, is titled
`[Issue-4593]` and belongs to a row that already has 1.3.54; and #4078's commit
`[Issue-4078] fix: fix minor condition` sits in `v1.3.24` but the issue closed as a **duplicate**, so
it never shipped as its own scope. The dashes here are WebApp rows, CMS and backend rows, research
rows and open rows — not missing evidence.

**Chainflip is the most-maintained provider in the epic**: 14 rows, more than Hydration and Uniswap
together. **KyberSwap has one.** A provider the wallet must understand — pairs, broker, slippage,
per-chain support — costs maintenance; an aggregator that understands itself does not.

**The first swap plan was entirely wrong about which venues would matter.** #38, #39 and #40 —
Zenlink, Acala, Parallel Finance — were opened on the same day in March 2022 as consecutive issue
numbers, and all three closed `NOT_PLANNED`. In-wallet swap shipped two years later through
Chainflip and Hydration, neither of which is on that list.

**The fee is the oldest unfinished thing in the epic.** Four of
[US-11.16](../sprints/stories/US-11.16-swap-flow-improvements.md)'s ten rows are about it — a wrong
fee (#3160), a fee that changes when the popup reopens (#3630), no total at all (#4376), and paying
it in a non-native asset (#1951, open since 2023). A cross-chain swap charges on both sides of the
hop plus the provider's cut, and nothing in the wallet has ever added those into one number.

---

## C. EPIC-33 (Maintenance — XCM & Bridge) merged into EPIC-13

**105 ledger stories dissolved; 115 rows across 17 stories.** The XCM & bridge ledger held one story
per tracker issue. It is deleted; its issues are now incremental-work rows on the capability they
belong to.

Arithmetic: 105 ledger issues − 3 umbrellas + 12 named in EPIC-13's prose and held by no table
+ 1 the ledger had merged into another row (#4299) = **115 rows**. One further recovery, #4606, was
**not** claimed — it already has an owning row in the still-unfolded Network & Token ledger.

### The largest scope-never-reached-a-table find yet

**Twelve issues named in EPIC-13's own prose were owned by no row** — every one of them open:

| Story | Issues | |
|---|---|---|
| [US-13.11](../sprints/stories/US-13.11-xcm-runtime-upgrade-paraspell-version-hardening.md) | #4108, #4255, #4405, #4424, #4622, #4636, #4756, #4798 | claim-step and provider defects |
| [US-13.8](../sprints/stories/US-13.8-bittensor-alpha-token-bridges.md) | #4903, #4919 | alpha-token bridges |
| [US-13.9](../sprints/stories/US-13.9-hyperbridge.md) | #4605, #4940 | Hyperbridge — **two issues for one integration**, neither closing the other |

US-13.11 was the worst case: a story whose entire scope was asserted in prose, cited in its
acceptance criteria, and provable in no table. It now carries eight rows, and **five of the eight
are the claim step rather than the bridge step** — the half of an asynchronous bridge where the
user's funds are already in flight. #4108 is `REOPENED`, the only such row in any fold so far: a
developer closed it and someone re-opened it.

### Six new stories, and one of them is a third of the area

| New story | Rows | |
|---|---|---|
| [US-13.12](../sprints/stories/US-13.12-xcm-route-and-channel-coverage.md) | 36 | make one more corridor work |
| [US-13.13](../sprints/stories/US-13.13-xcm-channel-operations-and-health.md) | 10 | watch the corridors, hide the broken ones |
| [US-13.14](../sprints/stories/US-13.14-xcm-fee-dry-run-and-validation.md) | 17 | what it costs and whether it arrives |
| [US-13.15](../sprints/stories/US-13.15-xcm-ui-history-and-token-selection.md) | 9 | the screen, the warning, the history entry |
| [US-13.16](../sprints/stories/US-13.16-asset-hub-migration-and-relay-chain-wind-down.md) | 4 | surviving AHM |
| [US-13.17](../sprints/stories/US-13.17-open-xcm-route-and-bridge-coverage.md) | 6 | corridors asked for, never delivered |

**XCM's cost is not the protocol, it is the corridor count.** 36 of 115 rows are one chain pair or
one asset on one route. Acala appears three times (#22 in 2022, #695, #3725 in 1.3.31), Astar three
times, Kintsugi three times — a corridor is delivered, then re-delivered every time the parachain
upgrades its runtime.

**"Recheck XCM Weekly" is a tracker issue** (#1598), with no scope, no release and no finish. So are
#2171 and #2579. A standing chore filed as a ticket is what happens when correctness depends on two
hundred chains that upgrade on their own schedules — and it is why the runtime channel toggle
([AD-09](../ARCHITECTURE.md#architecture-decisions)) exists.

**1.3.31 is the release where the wallet stopped predicting and started asking.** Five rows land in
it together — integrate the dry-run API (#3615), dry-run XCM (#4134), estimate the delivery fee
(#4133), validate the recipient (#4233), *"making sure teleports don't fail"* (#4387). Its precursor
#2792, *"Handle XCM delivery fee"*, closed `NOT_PLANNED` four releases earlier: the right question
with no available answer.

**Asset Hub Migration is disable-and-restore, not prevention.** #4632 — *prevent* cross-chain
transfers from breaking — closed **without shipping**. What shipped was #4787 (re-point the DOT/KSM
parameters, 1.3.63) and #4822 (re-enable, 1.3.66). Roughly six weeks in which relay-chain XCM was
simply not offered. For an ecosystem event the wallet does not control, that is the honest strategy.

### A fourth failure mode of hand-written changelog references

The hardware fold found mis-numbered bullets; the swap fold found PRs cited as issues. This one adds
**bullets whose text describes different work than the issue they cite**:

| Bullet | Cites | The issue actually is |
|---|---|---|
| `Fix bug not showing popup Swap confirmation when swap with Injected account (#3230)` @1.2.12 | #3230 | *"WebApp — add support XCM for PAH ↔ ETH (Snowbridge)"* — and its commits (`[Issue 3230] fix: estimate gas for snowbridge xcm`) are in `v1.2.12`, so the **version is right and the sentence is wrong** |
| `Support Across bridge (#3918, #4299)` @1.3.31 | #4299 | *"Handle functions to get address by chain or token when integrating Bitcoin"*, closed `NOT_PLANNED` — unrelated to Across |
| `Remove MYTH (PAH → Ethereum) (#4444)` @1.3.55 | #4444 | *"Hide some XCM channels"* — the issue was **widened after** the note was written |
| `Add support new XCM channels (#1457)` | #1457 | *"Add XCM DOT, USDT for some chains after migrated to ParaSpell V4"* — same, re-scoped later |

The last two are not errors: an issue title is live and a changelog line is frozen. **The tracker
title is the one used in every table here**, because it is the only one that can still be checked.

### A new check earned: the epic table and its own stories can disagree

Folding takes the version from the ledger's epic table. That table can be **stale relative to the
story files it links to**:

> **#526** showed `Shipped: —` in EPIC-33's table while its own story carried `version_shipped:
> 0.5.5`, proved through PR #523 titled `[Issue 411 + 526]` — a multi-issue tag a single-number scan
> missed.

Taking the table at face value would have silently dropped a release. The same check was run
retroactively against the two folds already pushed: **EPIC-31 had none, and EPIC-36's single case
(#2782 → 1.1.55) had already been recovered independently** through the linked-PR path. Nothing
escaped, but only by luck — this check now runs before the routing is built.

### Three rule-12 corrections, all one direction

#1264, #3252 and #3416 were `⏸ deprecated` in the ledger and are `done` here — all three closed
`COMPLETED`, two with board `Done`. #3416 matters most: *"Implement SnowBridge SDK to prevent asset
loss"* read as never-shipped would say the wallet still hand-rolls Snowbridge calls with assets at
risk.

### Board noise again, and what it would have broken

Six of umbrella #4157's twenty-one sub-issue links are not children — #60, #331, #412, #425, #468
and **#442, which is a pull request** (`[Issue-425] Fix UI`). Filtering them before applying rule 10
is what keeps #4267 and #4278 as rows rather than false umbrellas. Third area, same pattern.

### #402 and #418 are the same issue, twice

Word-for-word identical titles, sixteen issue numbers apart. One closed `NOT_PLANNED`, the other
shipped, and the generated ledger carried both as separate stories without noticing — the same shape
as #4161/#4162 in [US-7.2](../sprints/stories/US-7.2-transferable-vs-locked-balance-calculation.md).

### Flags for later folds

- **#4606** *"Migrate to ParaSpell V4 & update asset metadata"* (1.3.55) belongs to this epic —
  [AD-18](../ARCHITECTURE.md#architecture-decisions) is EPIC-13's — but is owned by a story in the
  **Network & Token** ledger. Route it when EPIC-24 folds.
- **US-13.10** (Axelar) has no tracker issue at all. The only Axelar issue in the repo, #1521, is a
  *swap* request and lives in [US-11.17](../sprints/stories/US-11.17-swap-coverage-expansion.md).
