---
id: US-10.2
title: "Substrate inject API (injectedWeb3)"
epic: EPIC-10
status: done
priority: P1
points: 5
sprint: sprint-2022-M02
version_shipped: 0.2.1
prd_ref: [FR-95]
arch_ref: [AD-21]
depends_on: [US-10.7]
assignee: jacogr
commit: 0b6bcd2bb9, f241e7191d
created: 2026-06-12
updated: 2026-06-12
---

## Goal

A Polkadot-ecosystem dApp (Polkadot-JS apps, Subsquare, Hydration, any
`@polkadot/extension-dapp` consumer) discovers and enables SubWallet through the
standard `window.injectedWeb3` interface, lists the user's authorized Substrate
accounts, and requests signatures — exactly as it would for the polkadot-js
extension. Substrate dApps work with SubWallet out of the box.

## Background

The Polkadot dApp ecosystem standardized on the polkadot-js injection interface:
an extension populates `window.injectedWeb3[<name>]` with `enable()`, and the
enabled object exposes `accounts.get()` / `accounts.subscribe()` and a `signer`
implementing `signPayload` / `signRaw`. SubWallet ships this via
`@subwallet/extension-inject` (the generic injector that populates
`window.injectedWeb3`), so any dApp using `@polkadot/extension-dapp`'s
`web3Enable` / `web3Accounts` / `web3FromSource` finds it without code changes.

`enable(origin)` and every signature request are gated by the per-origin
authorization store (US-10.7) and routed to the Substrate request handler in
`RequestService` ([AD-21](../../ARCHITECTURE.md#architecture-decisions)) behind the
shared `PopupHandler`. Substrate has two signing shapes — `signPayload` (extrinsic
payload) and `signRaw` (arbitrary bytes) — both surfaced through the approval
queue; the actual signing runs in the background keyring (AD-04).

Materializes [FR-95](../../PRD.md#functional-requirements). **Retroactive** — already shipped; `commit` /
`version_shipped` are backfilled during version reconciliation.

## Acceptance criteria

- [x] **AC-1** — **Given** any page, **When** it loads, **Then**
  `window.injectedWeb3` contains the SubWallet entry exposing an `enable()` that
  returns an injected object with `accounts` and `signer`.
- [x] **AC-2** — **Given** a dApp calling `web3Enable(...)` from an unauthorized
  origin, **When** it runs, **Then** the enable request is surfaced in the approval
  popup; only on approval does `accounts.get()` return the user-selected accounts.
- [x] **AC-3** — **Given** an enabled dApp, **When** it calls
  `signer.signPayload(payload)` or `signer.signRaw(data)`, **Then** the request is
  routed to the Substrate request handler and surfaced for approval; the signature
  is produced in the background keyring.
- [x] **AC-4** — **Given** an enabled dApp subscribed via `accounts.subscribe`,
  **When** the authorized account set changes, **Then** the dApp receives the
  updated account list.
- [x] **AC-5** — **Given** an unauthorized origin or a user rejection, **When** the
  request resolves, **Then** `enable()` / sign rejects with an error and no account
  list or signature leaks.

## Tasks

- [x] **TASK-10.2.1** — Populate `window.injectedWeb3` with the SubWallet entry + `enable()` (AC: 1) — `@subwallet/extension-inject`
- [x] **TASK-10.2.2** — Gate `enable()` on the per-origin authorization store; return only authorized accounts (AC: 2, 5)
- [x] **TASK-10.2.3** — Route `signPayload` / `signRaw` to the Substrate request handler behind `PopupHandler` (AC: 3) — `services/request-service/handler/*` (AD-21)
- [x] **TASK-10.2.4** — `accounts.subscribe` updates on authorized-set change (AC: 4)

## Dev notes

### Architecture constraints

- [AD-21](../../ARCHITECTURE.md#architecture-decisions) — `signPayload` / `signRaw` route to the Substrate handler behind the shared `PopupHandler`.
- [AD-04](../../ARCHITECTURE.md#architecture-decisions) (owned by EPIC-3) — signing runs in the background keyring; the injected `signer` carries no key bytes.
- This story does NOT introduce new AD entries.

### Cross-story dependencies

- Builds on [US-10.7](US-10.7-dapp-authorization-ui-per-origin.md) — `enable()` and `accounts.get()` honor the per-origin authorization.
- Builds on EPIC-2 US-2.7 — enqueues through `RequestService` / `PopupHandler`.
- Sibling [US-10.8](US-10.8-arbitrary-message-signing.md) — `signRaw` is the Substrate arm of arbitrary message signing.

### Sizing

Sized 5: one external standard surface (the polkadot-js injection interface) —
`injectedWeb3` discovery + `enable` gating + the dual `signPayload`/`signRaw`
signing shapes + account subscription, for one ecosystem.

### References

- [Source: PRD FR-95](../../PRD.md#functional-requirements) — Substrate inject API (`window.injectedWeb3`)
- [Source: ARCHITECTURE AD-21](../../ARCHITECTURE.md#architecture-decisions)
- [Source: ARCHITECTURE §Packages — extension-inject / extension-dapp](../../ARCHITECTURE.md)
- [Source: story-decomposition map — EPIC-10 US-10.2](../../../tmp/canonical/story-decomposition-map-v2.md)

## Verification commands

| AC | Command |
|---|---|
| AC-1 | Manual: load any page → `Object.keys(window.injectedWeb3)` includes SubWallet; `enable` is a function |
| AC-2 | Manual: `web3Enable` from a new origin → approval popup; `web3Accounts()` returns accounts only after approve |
| AC-3 | Manual: `signer.signPayload(...)` → approval popup; signature returned on approve |
| AC-4 | Manual: change authorized accounts in wallet → subscribed dApp account list updates |
| AC-5 | Manual: reject enable → rejection error, no accounts; unauthorized `accounts.get()` rejects |

## Changelog entry

### Added
- Substrate dApp inject API: `window.injectedWeb3` with `enable()`, account
  subscription, and a `signPayload` / `signRaw` signer routed through the Substrate
  request handler — full `@polkadot/extension-dapp` compatibility.

**Commit**:

## Implementation notes

Backfilled by US-21.2 (multi-agent trace + adversarial verify, run `wf_6b56f4cd-d08`; trace confidence: medium, rule: first-delivery).

**Evidence:** CHANGELOG '## [0.2.1] — 2019-07-12 — "First release to Chrome and FireFox stores, basic functionality only"' is the earliest delivery: window.injectedWeb3 was in the initial commit 0b6bcd2bb9 (2019-05-20) and enable() gating in f241e7191d (#20, 2019-05-23), both verified ancestors of the changelog-recorded 0.2.1 release commit 1e96fdce1d; later bullets like 0.5.1 "Change the injected Signer interface..." only prove prior existence. No v0.2.1 tag exists in this repo (v0.2.x tags are SubWallet's restarted 2022 versioning), so ancestry was checked against the recorded release commit — hence medium, not high.

Commits `0b6bcd2bb9, f241e7191d` verified contained in the v0.2.1 anchor via `git merge-base --is-ancestor`; assignee resolved through the [US-21.1 contributor map](../../notes/contributor-map.md).

## Cross-references

- [PRD FR-95](../../PRD.md#functional-requirements)
- [Epic EPIC-10](../epics/EPIC-10.md)
- [US-10.7](US-10.7-dapp-authorization-ui-per-origin.md) · [US-10.8](US-10.8-arbitrary-message-signing.md) · [US-10.9](US-10.9-dapp-createtransaction-api-rfc-6213.md)
