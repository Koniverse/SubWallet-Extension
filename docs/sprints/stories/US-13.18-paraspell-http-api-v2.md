---
id: US-13.18
title: "ParaSpell HTTP API v2 migration"
epic: EPIC-13
status: done
priority: P2
points: 2
sprint: sprint-2026-W34
version_shipped: 1.3.88
prd_ref: [FR-126, NFR-16]
arch_ref: [AD-19, AD-18]
depends_on: [US-13.14]
assignee: tunghp2002
commit: 1ee6165fab, da2207be1b
created: 2026-08-10
updated: 2026-08-25
---

## Goal

Move every ParaSpell HTTP call the XCM path makes from the provider's `/v1` API to `/v2`, so
XCM build, fee, dry-run, dry-run-preview and transferable-amount keep working when `/v1` is
retired.

## Status

> **👀 review — written, not merged, not shipped.** PR
> [#5053](https://github.com/Koniverse/SubWallet-Extension/pull/5053)
> (`koni/dev/issue-5051` → `subwallet-dev`) is **open**; the board (project #2) has
> [#5051](https://github.com/Koniverse/SubWallet-Extension/issues/5051) at **In Testing** in its
> live `Week 32 - 2026` iteration. `version_shipped` and `commit` are **empty on purpose**: the
> branch commit `1ee6165fab` is not an ancestor of `origin/subwallet-dev`, and a `commit:` names
> what made a capability true ([D106](../../CONTEXT.md)). Nothing is true yet.

### Shipped 2026-08-19 — v1.3.88

> **✅ done.** PR [#5053](https://github.com/Koniverse/SubWallet-Extension/pull/5053) merged
> **2026-08-18** (`da2207be1b`) and went out in **v1.3.88** on 2026-08-19.
> [#5051](https://github.com/Koniverse/SubWallet-Extension/issues/5051) closed the same day.

Lineage checked, not assumed: `git merge-base --is-ancestor da2207be1b 93734db9cb` passes, and
`git tag --contains da2207be1b` returns exactly `v1.3.88`.

`commit:` names two — `1ee6165fab`, the `/v2` switch that made the capability true on the branch,
and `da2207be1b`, the merge that made it true in the product ([D106](../../CONTEXT.md)). The PR
carried **8 commits** in the end, one more than the 7 counted on 08-18.

**The release carried more than this story.** v1.3.88 also ships a chainlist update (PRDCTR chain,
TUSDT symbol) and the removal of XCM routes for chains ParaSpell dropped in v2 — the latter is a
direct consequence of this migration but is **not** this story's scope. Both have their own QC
pages, [US-42.16](US-42.16-qc-chainlist-update-prdctr-chain-and-tusdt-symbol.md) and
[US-42.18](US-42.18-qc-issue-705-remove-dropped-xcm-routes.md).

**On the approval gap noted below:** it was never closed. The two approvals still date from
2026-08-10 and five of the eight commits landed after them; the PR merged without a re-review of
the final head. Recording it because it was flagged before the merge, not after — see
[notes/2026-08-25.md](../../notes/2026-08-25.md).

### Resync 2026-08-18 — still `review`, but QC has started

Re-read from `gh pr view` / `gh issue view`, **not** the board (the `read:project` scope is still
unavailable — [2026-08-13 §D](../../notes/2026-08-13.md)). The status does not change; what it
rests on does:

| | 2026-08-13 | 2026-08-18 |
| --- | --- | --- |
| #5051 | OPEN, untouched | OPEN, **commented 08-18** |
| PR #5053 | open, unmerged | open, unmerged — last commit `a58890eefa` (08-17) |
| Commits | — | 7, +110 / −64 |
| Reviews | — | 2 × APPROVED by `lw-cdm`, **both 2026-08-10** |
| QC | none | **done** — [US-42.17](US-42.17-qc-issue-5051-paraspell-api-v2.md) closed 2026-08-18; re-verified at release gate [US-42.19](US-42.19-qc-release-extension-v1-3-88.md) |

Two things worth naming:

- **The approvals are older than the code.** Both were submitted 2026-08-10; four of the seven
  commits landed after — including the last one, a type-prefix rename. Nobody has approved the
  current head.
- **AC-2 through AC-5 are exactly what QC exercised**, on a build, by hand. That does not tick them:
  these ACs are claims about `/v2` behaviour across six endpoints, while the QC pass covers XCM
  transfer and a single-chain recheck. QC evidence and an AC tick are different claims — the QC page
  is [US-42.17](US-42.17-qc-issue-5051-paraspell-api-v2.md), `done` on 2026-08-18 with a stated
  verdict, re-verified across all four build stages at [US-42.19](US-42.19-qc-release-extension-v1-3-88.md).
  The ACs here stay unticked until someone checks `/v2` on all six endpoints.

## Background

### Two different "ParaSpell versions" — do not conflate them

[US-13.14](US-13.14-xcm-fee-dry-run-and-validation.md) already carries three ParaSpell rows, and
they are not all the same axis:

| Issue | Shipped | What actually moved |
| --- | --- | --- |
| [#4606](https://github.com/Koniverse/SubWallet-Extension/issues/4606) | 1.3.55 | **SDK** — ParaSpell V4 |
| [#4908](https://github.com/Koniverse/SubWallet-Extension/issues/4908) | 1.3.72 | **SDK** — ParaSpell V5 |
| [#4979](https://github.com/Koniverse/SubWallet-Extension/issues/4979) | 1.3.79 | **HTTP API** — the hosted endpoint path, `/v1` |
| [#5051](https://github.com/Koniverse/SubWallet-Extension/issues/5051) | — | **HTTP API** — `/v1` → `/v2`. *This story.* |

The SDK is a package version in `package.json`; the HTTP API is a path prefix on the hosted
service SubWallet proxies to. #5051 is the direct successor of **#4979**, not of #4908.

### What the wallet calls

`packages/extension-base/src/services/balance-service/transfer/xcm/utils.ts` holds one
`const version` that prefixes all six ParaSpell endpoints — `x-transfer`, `xcm-fee`, `dry-run`,
`dry-run-preview`, `transferable-amount`, `min-transferable-amount`. Every call goes out through
`ProxyServiceRoute.PARASPELL`, so the provider key stays server-side (NFR-16, AD-19) and the
version prefix is the *only* client-side coupling to the provider's API surface.

That is why the change is one line — and why it is not small. Those six endpoints back XCM
transfer construction, fee estimation and the dry-run preview that
[US-11.8](US-11.8-cross-chain-swap-routing.md) uses to validate swap-bridge steps. A silent
response-shape change between `/v1` and `/v2` surfaces as a broken XCM quote, not as a build error.

## Acceptance criteria

- [x] **AC-1** — **Given** the XCM transfer path, **When** it calls ParaSpell, **Then** every one
  of the six endpoints is requested under `/v2`. *(Verified in the merged tree:
  `const version = '/v2'` at `xcm/utils.ts:115`, consumed by all six — `x-transfer`, `xcm-fee`,
  `dry-run`, `dry-run-preview`, `transferable-amount`, `min-transferable-amount` (lines 118–123).)*
- [x] **AC-2** — **Given** `/v2`, **When** an XCM transfer is built and submitted on a live route,
  **Then** it succeeds and the destination receives the tokens.
  *([US-42.17](US-42.17-qc-issue-5051-paraspell-api-v2.md), 10/10 AC on fresh install + upgrade.)*
- [x] **AC-3** — **Given** `/v2`, **When** `xcm-fee`, `transferable-amount` and
  `min-transferable-amount` are queried, **Then** each returns a usable figure and no route
  regresses to *unavailable* (NFR-22). *(US-42.17.)*
- [x] **AC-4** — **Given** `/v2`, **When** the swap-bridge dry-run preview runs
  ([US-11.8](US-11.8-cross-chain-swap-routing.md) AC path, #4671), **Then** validation still
  rejects the cases it rejected under `/v1`. *(US-42.17.)*
- [x] **AC-5** — **Given** a `/v2` error response, **When** it is handled, **Then** the
  `ParaSpellError` shape (`message`, `error`, `statusCode`) still parses — or the parser is
  updated with it. *(Updated, not merely re-checked: `948d6e47c2` rewrote v2 error handling.
  `interface ParaSpellError` at `xcm/utils.ts:109`, applied at four call sites — 229, 274, 317, 404.)*

> **Ticked 2026-08-25, after the merge — not before.** On 2026-08-18 this block read *"nothing is
> ticked, deliberately"*, because `dev` still called `/v1` and AC-2…AC-5 were claims about a running
> network no repository check could settle. Both conditions changed: PR #5053 merged (`da2207be1b`,
> shipped **v1.3.88**) so AC-1 is now a fact about *this* tree, and QC completed with a stated
> verdict so AC-2…AC-4 rest on evidence rather than on a build having been exercised.
>
> **AC-2…AC-4 are ticked on QC evidence, not on a repository check** — a distinction
> [D107](../../CONTEXT.md) cares about. Nothing here re-verifies a live route; if
> [US-42.17](US-42.17-qc-issue-5051-paraspell-api-v2.md) is wrong, these ticks are wrong with it.

## Tasks

- [x] Point the shared `version` prefix at `/v2` *(done on the branch, `1ee6165fab`)*
- [x] Regression-test the six endpoints against live routes *(QC ran on a build, [US-42.17](US-42.17-qc-issue-5051-paraspell-api-v2.md))*
- [x] Confirm the swap-bridge dry-run preview path is unaffected *(covered by US-42.17)*
- [x] Confirm the error shape, or update `ParaSpellError` *(`948d6e47c2` — new v2 error handling)*
- [x] Merge PR #5053 and record `commit:` + `version_shipped:` *(merged 2026-08-18, shipped 1.3.88)*

## Dev notes — References

- Issue [#5051](https://github.com/Koniverse/SubWallet-Extension/issues/5051) — no sub-issues, no
  parent (`gh api …/issues/5051` → `total=0`, `parent: none`). A leaf, so it is a story row and
  not an epic-level umbrella ([AGENTS.md](../../../AGENTS.md) rule 10).
- PR [#5053](https://github.com/Koniverse/SubWallet-Extension/pull/5053), branch
  `koni/dev/issue-5051`, commit `1ee6165fab` — **open, unmerged**.
- Predecessor [#4979](https://github.com/Koniverse/SubWallet-Extension/issues/4979) (API v1,
  1.3.79) in [US-13.14](US-13.14-xcm-fee-dry-run-and-validation.md).
- [PRD NFR-16](../../PRD.md#non-functional-requirements) — ParaSpell is one of the keyed providers
  proxied through the SubWallet backend; the client never holds the key.
- Discovered during the 2026-08-10 board sync — #5051 was the one live board item no story or epic
  cited. See [notes/2026-08-10.md §D](../../notes/2026-08-10.md).

## Verification commands

```bash
XCM=packages/extension-base/src/services/balance-service/transfer/xcm/utils.ts

# AC-1 is UNTICKED because this prints /v1 — the shipped tree still calls v1
grep -n "^const version" $XCM

# ...while the branch prints /v2. This is the whole change.
git show koni/dev/issue-5051:$XCM | grep -n "^const version"

# All six endpoints inherit the one prefix, so the one line moves all of them (expect: 6)
grep -cF '${version}/' $XCM

# Status honesty — this is what keeps commit:/version_shipped: empty and the ACs open
git merge-base --is-ancestor 1ee6165fab origin/subwallet-dev \
  && echo "MERGED — tick AC-1, set commit: + version_shipped:, move to done" \
  || echo "unmerged — commit:/version_shipped: correctly empty, ACs correctly open"
```

## Changelog entry

- Update ParaSpell HTTP API integration to v2 (#5051, US-13.18)
