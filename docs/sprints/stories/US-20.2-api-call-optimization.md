---
id: US-20.2
title: "Request economy — in-flight dedup, app-wide cap, notification-fetch flood"
epic: EPIC-20
status: in-progress
priority: P1
points: 5
sprint: sprint-2026-W28
version_shipped:
prd_ref: [NFR-20, NFR-21]
arch_ref: [AD-24, AD-25]
depends_on:
assignee: nulllpc
commit:
created: 2026-06-12
updated: 2026-07-15
---

## Status refresh — 2026-07-15

> Synced from GitHub Projects board #2 ("SubWallet.App – Development"): issue #4447 is **Implementing** there, so this story moves `backlog` → `in-progress` (sprint `sprint-2026-W28`). Only status/sprint changed; Goal, AC and reasoning below are untouched. The board is the live source for workflow state.

## Goal

Stop the request waste the shipped work did **not** cover: two identical requests fired in
the same tick, a cap that only guards six consumers while notification/price/SDK/RPC
traffic bypasses it entirely, and an **unbounded pagination loop** that fires 4× per address
across every account, every 30 minutes.

## Background

**Narrowed on 2026-07-14.** This story used to be `in-progress`, carrying both work that had
shipped under an issue no story cited **and** work nobody has ever started. **A story is the
unit of status: if two halves have different truths, it is not one story**
([CONTEXT D100](../../CONTEXT.md)).

The shipped half left for two retroactive stories:

| Left for | What it was |
| --- | --- |
| [US-20.8](US-20.8-api-request-strategy-v2.md) — `done` 1.3.47, `S2kael` | `api-request-strategy-v2` — md5-keyed 60 s response cache, group cancellation, per-window cap, adaptive backoff (**#4448**; the CHANGELOG had it as #4458) |
| [US-20.9](US-20.9-aggregated-data-via-services-sdk.md) — `done` 1.3.52, `bluezdot` | aggregated data routed through the external Services SDK, 18 call sites (**#4465**) |

**What is left here has no code.** This story's own two issues — **#4447** and **#4021** —
have **zero commits**, and the umbrella perf tracker `#4197` has sat at zero for about a
year. `backlog`, and `assignee` is empty **on purpose**: an unowned story with a name on it
is worse than an unowned one, because it looks handled.

**The sharpest item is AC-3.** `fetchAllAvailBridgeClaimable` is an unbounded
`while (isContinue)` pagination loop — no page cap, no rate limit — fired **4× per address
across every Substrate and EVM address on a 30-minute cron**, with a bare
`.then().catch(console.error)`. It is live in v1.3.83. This is the cheapest real win in
EPIC-20 and the one recommended for the 3 points freed by deprecating
[US-20.3](US-20.3-read-path-memory-budget.md) ([CONTEXT D96](../../CONTEXT.md)).

Defends [NFR-20](../../PRD.md#non-functional-requirements) and
[NFR-21](../../PRD.md#non-functional-requirements) on the paths the shipped work left
uncovered; owns no FR.

## Acceptance criteria

> **Narrowed 2026-07-14.** The two ACs the audit found **already met** (per-window cap;
> aggregate data via the SDK) left this story for
> [US-20.8](US-20.8-api-request-strategy-v2.md) / [US-20.9](US-20.9-aggregated-data-via-services-sdk.md).
> Everything below is **verified absent from the v1.3.83 tree**.

- [ ] **AC-1** — **Given** two callers requesting the same resource, **When** the second is made while the first is still in flight, **Then** they **share one request**. *(Today `api-request-strategy-v2` writes `cacheMap` **after** `run()` resolves — so two identical requests in the same tick both miss and both fire. It is a post-hoc response cache, not single-flight, and it is Subscan-only.)* (#4447, NFR-20)
- [ ] **AC-2** — **Given** a burst of requests **anywhere in the app**, **When** they are issued, **Then** an **app-wide** per-window cap holds and excess is coalesced. *(Today the cap is real but scoped to the six strategy consumers — Subscan, Runes, Hiro, Bitcoin mainnet/testnet. **Notification, price, SDK and RPC traffic bypass it entirely.**)* (#4447)
- [ ] **AC-3** — **Given** the notification fetch, **When** it runs, **Then** it is **bounded** (page cap + rate limit) and cannot suspend other requests. *(Today `fetchAllAvailBridgeClaimable` is an **unbounded `while (isContinue)` loop**, fired 4× per address across every Substrate and EVM address on a 30-minute cron, errors swallowed by `.catch(console.error)`. **Live in v1.3.83.**)* (#4021)
- [ ] **AC-4** — **Given** the SDK request path or the notification / bridge-indexer path, **When** a request fails, **Then** a **stale cached value** is served where available. *(Today fallback exists only for *static* data — `fetchStaticData` → bundled JSON, `fetchStaticCache` → default behind a 9 s timeout. The SDK path has no stale fallback; the notification / bridge-indexer path has **neither retry bounding nor fallback**.)*
- [ ] **AC-5** — **Given** the request layer, **When** a change touches it, **Then** a harness asserts request counts, in-flight duplicates and the per-window cap, failing the build on a regression. *(**No such spec exists.** [US-20.4](US-20.4-many-account-submit-performance.md) depends on this harness.)*

## Tasks

- [ ] **TASK-20.2.1** — In-flight dedup: coalesce identical concurrent requests onto one shared promise (write `cacheMap` **before** `run()` resolves, not after) (AC: 1)
- [ ] **TASK-20.2.2** — Lift the per-window cap to be **app-wide**, covering notification / price / SDK / RPC traffic — not just the six strategy consumers (AC: 2)
- [ ] **TASK-20.2.3** — Bound `fetchAllAvailBridgeClaimable`: page cap + rate limit + real error handling; stop the 4×-per-address, every-30-minutes flood (AC: 3) — *(#4021)*
- [ ] **TASK-20.2.4** — Stale-value fallback on the SDK path; bounded retry **and** fallback on the notification / bridge-indexer path (AC: 4)
- [ ] **TASK-20.2.5** — Regression harness: request counts, zero duplicate in-flight requests, the app-wide cap, and a standing test reproducing the #4021 flood (AC: 1, 2, 3, 5)

## Dev notes

### Architecture constraints

- [AD-24](../../ARCHITECTURE.md#architecture-decisions) — aggregated balance/fee/swap/XCM/NFT data rides the Services SDK backend; this story enforces that the client routes through it rather than fanning out per chain. The SDK itself is owned by [EPIC-2](../epics/EPIC-2.md).
- [AD-25](../../ARCHITECTURE.md#architecture-decisions) — market/metadata/media goes through the `api-cache` / `static-data` / `ipfs-files` proxies; this story enforces routing, not the proxy build.
- This story does NOT introduce new AD entries.

### Cross-story dependencies

- Builds on [US-20.8](US-20.8-api-request-strategy-v2.md) — the shipped request strategy this story extends: turn its post-hoc cache into single-flight, and lift its per-consumer cap to app-wide.
- Builds on [US-20.9](US-20.9-aggregated-data-via-services-sdk.md) — the shipped SDK migration. It moved the *aggregate* reads; the notification / bridge-indexer path it left behind is where the flood lives.
- Sibling [US-20.1](US-20.1-core-structure-and-lifecycle-refactor.md) — also `backlog`, also unowned. The idle-quiet lifecycle invariant and this no-redundant-fan-out invariant must agree on when the worker may talk to the network.
- Required by [US-20.4](US-20.4-many-account-submit-performance.md) — that story reuses the **request-count budget harness** set up here for the many-account submit path.

### Performance budget

- Zero duplicate in-flight requests; a per-window request cap holds.
- No request class may starve the others — the notification fetch must not suspend other requests, and the extension stays openable while it runs (#4021).
- Measured by the request-count budget harness (TASK-20.2.6).
- Story PR description must explicitly confirm these budgets are met.

### What we explicitly did NOT do

- No change to what the data *means* — correctness of balances/fees/routing stays with the engine/feature epics; this story only changes *how many* requests fetch it and *in what order*.
- No new backend — the Services SDK (AD-24) and proxies (AD-25) are consumed, not built (owned by [EPIC-2](../epics/EPIC-2.md)).

### Points justification

5 pts — §3a-bis "production deliverable / one external system integration":
request-dedup + per-window cap + class prioritization across the request layer,
plus a standing regression for the #4021 flood. Bounded, single-layer
optimization touching the request/SDK/proxy seam — squarely a 5, below the
multi-system 8.

### References

- [Issue #4447](https://github.com/Koniverse/SubWallet-Extension/issues/4447) — redundant API fan-out / per-window request volume
- [Issue #4021](https://github.com/Koniverse/SubWallet-Extension/issues/4021) — notification-fetch flood suspends other requests; extension can't open
- [Source: PRD NFR-20, NFR-21](../../PRD.md#non-functional-requirements)
- [Source: ARCHITECTURE AD-24, AD-25](../../ARCHITECTURE.md#architecture-decisions)

## Verification commands

| AC | Command |
|---|---|
| AC-1, AC-2 | Test: request-count budget harness asserts no duplicate in-flight request + per-window cap holds |
| AC-3 | Test: reproduce notification flood (#4021) → other requests not suspended, extension openable; Manual: open extension while notifications fetch |
| AC-4 | Code: aggregated data goes through the Services SDK, market/metadata/media through `api-cache`/`static-data`/`ipfs-files`; no direct per-chain fan-out |
| AC-5 | Test: backend/proxy failure → bounded retries, stale-on-error fallback, no retry storm |

## Changelog entry

### Fixed
- Notification fetch no longer floods the request channel or suspends other requests; the extension stays openable while notifications load (root cause: unbounded, un-prioritized notification request class, #4021).

### Changed
- Identical concurrent requests are coalesced (no duplicate in-flight requests) and a per-window request cap bounds fan-out, routing aggregated data through the Services SDK and market/metadata/media through the cache/CDN proxies (#4447).

**Commit**:

## Implementation notes

**Narrowed 2026-07-14 ([CONTEXT D100](../../CONTEXT.md)).** The audit below is the evidence
that forced the split — it is kept because it is *why* this story now looks the way it does.
The **met** rows have left for [US-20.8](US-20.8-api-request-strategy-v2.md) /
[US-20.9](US-20.9-aggregated-data-via-services-sdk.md); the **not met** rows are the ACs
above.

**Audited 2026-07-13 (US-21.2 follow-up). Two of five ACs were already met by shipped
work that no story cited — and the CHANGELOG had the wrong issue number on the biggest
piece of it.**

**The story's own two issues never produced code**: `#4021` and `#4447` have **zero
commits**. But the request-economy work below *did* ship:

| Shipped | Release | What it actually did |
| --- | --- | --- |
| **`#4448`** (PR #4470) | **1.3.47** | The real "Optimize Subscan API request": `strategy/api-request-strategy-v2` — md5-keyed 60 s response cache, group request cancellation, a cache for `getMultiChainBalance`, and skipping reward-history fetches for addresses with no stake. **docs/CHANGELOG.md attributed this bullet to `#4458`, which is wrong** — #4458's commits are chain-list v0.2.108 bumps. Corrected in the changelog. |
| `#4465` | 1.3.52 | Deleted the in-repo `packages/subwallet-api-sdk` and routed aggregated data through the external `@subwallet-monorepos/subwallet-services-sdk` (18 call sites). **This is what satisfies AC-4 / NFR-20 / AD-24**, though its changelog bullet ("Apply sw version when request to Backend") badly undersells it. |
| `#4434` | 1.3.42 | "Optimize Request Handling in SendFund Form" — **only the 300 ms UI debounce shipped**. The Bitcoin-API-cache half of the issue is contained in **no tag** and sits unmerged on `koni/dev/issue-4434-v2`. |
| `#4623` | 1.3.54 | dTAO validator data moved behind a static cache — a request-flood fix. |
| `#4965` | 1.3.75 | User-configurable Subscan API key (raises the rate ceiling the #4448 backoff halves against). |

**AC-by-AC, against the v1.3.83 tree:**

- **AC-1 in-flight dedup — not met.** `api-request-strategy-v2`'s `cacheMap` is written *after* `run()` resolves, so two identical requests in the same tick both miss and both fire. It is a post-hoc response cache, not single-flight — and it is Subscan-only.
- **AC-2 per-window cap — met, pre-existing.** `BaseApiRequestContext` sets `callRate = 2` per `intervalCheck = 1000 ms`; the strategy slices to `callRate` each tick and queues the rest. Scope is the six strategy consumers (Subscan, Runes, Hiro, Bitcoin mainnet/testnet) — there is **no app-wide cap**; notification, price, SDK and RPC traffic bypass it.
- **AC-3 notification-fetch flood (#4021) — not met, and the flood is intact.** `fetchAllAvailBridgeClaimable` is an **unbounded `while (isContinue)` pagination loop** with no page cap and no rate limit, fired 4× per address across every Substrate and EVM address on a 30-minute cron with bare `.then().catch(console.error)`.
- **AC-4 aggregated data via the SDK + cache/CDN — met (via #4465).** Caveat: "no direct per-chain fan-out" is not fully true — `SubscanService` still calls `https://{chain}.api.subscan.io` directly, as does the bridge-indexer path.
- **AC-5 bounded retry + stale-on-error — half met.** Bounded retry is real (`maxRetry = 9`, `reduceLimitRate()` halves `callRate` for 30 s on a rate-limit error — a genuine adaptive backoff). Fallback exists only for *static* data (`fetchStaticData` → bundled JSON, `fetchStaticCache` → default behind a 9 s timeout). There is no stale-value fallback on the SDK request path, and the notification/bridge-indexer path has **neither** retry bounding nor fallback.
- **TASK-20.2.6 harness — does not exist.** No spec asserts request counts, in-flight duplicates or a per-window cap. US-20.4 depends on this harness.

## Cross-references

- [Issue #4447](https://github.com/Koniverse/SubWallet-Extension/issues/4447)
- [Issue #4021](https://github.com/Koniverse/SubWallet-Extension/issues/4021)
- [Epic EPIC-20](../epics/EPIC-20.md)
- [US-20.1](US-20.1-core-structure-and-lifecycle-refactor.md)
- [US-20.4](US-20.4-many-account-submit-performance.md)
