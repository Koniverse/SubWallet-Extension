---
id: US-14.2
title: "Fiat off-ramp: sell crypto for fiat"
epic: EPIC-14
status: done
priority: P2
points: 3
sprint:
version_shipped: 1.3.56
version_space: webapp
prd_ref: [FR-137]
arch_ref: [AD-19, AD-24]
depends_on: [US-14.1]
assignee: tunghp2002
commit: 6089c9c5af, 356b3a7655, ff7942bb84
created: 2026-06-12
updated: 2026-06-12
external_deps: [transak]
---

## Goal

A user can cash out — sell crypto they hold for fiat — without leaving SubWallet,
by being handed off to Transak's hosted sell flow with a pre-built order URL for
the right asset, network, and address. This closes the loop opened by the
on-ramp: money in (buy) and money out (sell) ride the same provider hand-off and
the same backend-minted order-URL path, differing only by the `action`.

## Background

Selling is the symmetric half of the fiat-gateway: the same partner (Transak)
that serves the on-ramp also serves the off-ramp via the web app, so the
off-ramp does **not** introduce a new provider or a new integration surface — it
reuses the Transak adapter and the Services-SDK order-URL path from
[US-14.1](US-14.1-fiat-on-ramp-buy-crypto-with-card.md), calling
`subwalletApiSdk.transakApi.generateOrderUrl({ ..., action: 'SELL' })` instead of
`'BUY'`. The partner secret stays server-side
([AD-19](../../ARCHITECTURE.md#architecture-decisions),
[AD-24](../../ARCHITECTURE.md#architecture-decisions), NFR-16 / NFR-20); SubWallet
never receives the fiat — Transak handles KYC, payout, and settlement.

Materializes [FR-137](../../PRD.md#functional-requirements). This story is **Retroactive** — already
shipped. `commit` / `version_shipped` are backfilled during version
reconciliation.

## Acceptance criteria

- [x] **AC-1** — **Given** a token the user holds that Transak supports for sell,
  **When** the user starts a sell, **Then** Transak's hosted sell flow opens with
  an order URL carrying `action: 'SELL'` and the correct asset, network, and
  source address.
- [x] **AC-2** — **Given** the sell order URL, **When** it is built, **Then** it is
  minted by the Services SDK (`transakApi.generateOrderUrl`) and **no** Transak
  secret credential is present in the shipped bundle (same invariant as the buy
  path).
- [x] **AC-3** — **Given** a token that Transak does not support for off-ramp,
  **When** the user opens the sell surface, **Then** the option is unavailable/
  disabled with guidance rather than an errored screen (unhappy path: no off-ramp
  support for the asset).
- [x] **AC-4** — **Given** the user's region is not serviceable for sell, or their
  KYC is rejected by Transak, **When** they reach the hosted flow, **Then** the
  rejection is handled by Transak and the user returns to the wallet without a
  partial or stuck order state (unhappy path: provider unavailable in region /
  KYC rejection).

## Tasks

- [x] **TASK-14.2.1** — Sell surface entry that reuses the Transak adapter with `action: 'SELL'` (AC: 1, 2)
  - [x] Call `transakApi.generateOrderUrl({ symbol, address, network, action: 'SELL' })`; no new provider adapter.
- [x] **TASK-14.2.2** — Gate sell availability by Transak off-ramp token support (AC: 3)
  - [x] Drive availability from the static-data roster; disable unsupported assets.
- [x] **TASK-14.2.3** — Unhappy-path handling: no off-ramp support / region or KYC rejection (AC: 3, 4)
  - [x] Disabled state for unsupported tokens; clean return from the Transak flow on rejection.

## Dev notes

### Architecture constraints

- [AD-24](../../ARCHITECTURE.md#architecture-decisions) — the sell order URL is built by the Services SDK backend (`transakApi.generateOrderUrl`), reusing the buy path's substrate with `action: 'SELL'`.
- [AD-19](../../ARCHITECTURE.md#architecture-decisions) / NFR-16 — the Transak partner secret stays behind the backend; the off-ramp adds no new bundled credential.
- This story does NOT introduce new AD entries.

### Cross-story dependencies

- Builds on [US-14.1](US-14.1-fiat-on-ramp-buy-crypto-with-card.md) — reuses the Transak `CreateBuyOrderFunction` adapter and the Services-SDK `generateOrderUrl` path; off-ramp is the same call with `action: 'SELL'`.

### What we explicitly did NOT do

- No second off-ramp provider — Transak only, per FR-137. Trigger to revisit: a second partner offering off-ramp coverage for SubWallet's assets/regions.
- No in-product payout/settlement — Transak owns KYC, fiat payout, and settlement; SubWallet never takes fiat custody.

### References

- [Source: PRD FR-137](../../PRD.md#functional-requirements) — fiat off-ramp (sell) via Transak web app
- [Source: PRD NFR-16](../../PRD.md#non-functional-requirements) — third-party API-key protection
- [Source: PRD NFR-20](../../PRD.md#non-functional-requirements) — SubWallet Services SDK backend
- [Source: ARCHITECTURE AD-19, AD-24](../../ARCHITECTURE.md#architecture-decisions)
- Code: `packages/extension-koni-ui/src/utils/buy/transak.ts` (shared BUY/SELL via `action`)

## Verification commands

| AC | Command |
|---|---|
| AC-1 | Manual: start a sell on a supported token → Transak sell flow opens with `action: SELL` and correct asset/network/address |
| AC-2 | `rg "generateOrderUrl" packages/extension-koni-ui/src/utils/buy/transak.ts` shows the shared Services-SDK call; no Transak secret in the bundle |
| AC-3 | Manual: open sell for a Transak-unsupported token → option disabled with guidance |
| AC-4 | Manual: select a non-serviceable region / trigger KYC rejection → Transak rejects and returns cleanly to the wallet |

## Changelog entry

### Added
- Fiat off-ramp: sell crypto for fiat via the Transak hosted flow, reusing the
  on-ramp's Services-SDK order-URL path with `action: 'SELL'` so the partner
  secret stays server-side.

**Commit**:

## Implementation notes

**Shipped in the WEB APP, not the extension** — `version_space: webapp` (US-21.2, 2026-07-13).

This repo builds two products on two branch lineages that **share a version-number
series but are not the same product**: the extension (`master` / `subwallet-dev`,
tagged `vX.Y.Z`, now 1.3.82) and the web app (`webapp` / `webapp-dev`, untagged —
its releases are `[CI Skip] release/stable X.Y.Z` commits, now 1.3.56). Extension
v1.3.56 and web-app 1.3.56 are *different releases*. This story shipped in **web-app
1.3.56** (release commit `c1301536b2`, 2025-09-11); all three commits verified with
`git merge-base --is-ancestor <sha> c1301536b2`, and `git tag --contains <sha>`
returns no `v*` tag for any of them.

**Evidence is tree-level, not changelog-level** — the webapp CHANGELOG has *no*
bullet for the off-ramp; the feature shipped silently. Ground truth: walking every
`release/stable` commit on `origin/webapp` and testing
`git ls-tree <rel> packages/extension-web-ui/src/Popup/OffRampLoading.tsx` shows the
file **absent in 1.2.28 → 1.3.54 and present only from 1.3.56**. At 1.3.56 the flow
is genuinely reachable: `utils/buy/transak.ts` builds the Transak URL with
`productsAvailed: action || 'BUY'`, and the header exposes "Buy & Sell".

**Two traps recorded so nobody re-derives them wrong:**
- The webapp 1.3.43 bullets "Update content for Buy & Sell tokens (#4456)" and
  "Update script scan list token support feature on/off-ramp (#4350)" look like the
  delivery — 1.3.43's tree has **zero** off-ramp files. Quoting them dates the
  feature ~3 months early.
- An extension port **was** written and abandoned: branch `koni/dev/issue-3843`
  (4 commits, May–Jun 2025) is in no tag and not even an ancestor of webapp 1.3.56.
  In the extension space this capability is *attempted, never shipped* — the
  extension at v1.3.82 has no off-ramp code at all.

## Cross-references

- [PRD FR-137](../../PRD.md#functional-requirements)
- [Epic EPIC-14](../epics/EPIC-14.md)
- [US-14.1](US-14.1-fiat-on-ramp-buy-crypto-with-card.md)
