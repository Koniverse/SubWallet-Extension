---
id: US-17.1
title: "Proxy types & authority management"
epic: EPIC-17
status: done
priority: P2
points: 5
sprint: sprint-2026-M01
version_shipped: 1.3.72
prd_ref: [FR-148]
arch_ref: [AD-16]
depends_on:
assignee: Thiendekaco
commit: da069575d07c1a13d7bd5597eaad5b2e931d9e16, 4be186b76c7b11c79a377611be4e10b1e62f97a5, 4c97547b8b31bb9ac584dffa746a9154f9343090
created: 2026-06-12
updated: 2026-06-12
---

## Goal

A power user can set up delegated authority on a Substrate account: add a proxy
account, choose which named Polkadot proxy-pallet type it holds (Any,
NonTransfer, Governance, Staking, …), and see and manage that delegated-authority
relationship. This is the *model* half of proxy — it establishes who may sign for
whom and under which type, so that the signing-time story
([US-17.2](US-17.2-proxy-signing-sign-selector-and-proxied-by-display.md)) has a
well-defined authority to enforce.

## Background

Proxy is the Polkadot `proxy` pallet's delegated-authority model: an account can
authorise another account to dispatch certain calls on its behalf, scoped to a
named proxy *type* that limits which calls are allowed (Any permits everything;
NonTransfer excludes balance transfers; Governance and Staking scope to those
domains; and so on). This story owns the account/authority **model**: adding a
proxy to a Substrate account, choosing the proxy type, and managing (viewing,
removing) the relationship. Per [AD-16](../../ARCHITECTURE.md#architecture-decisions)
the wallet supports **only the named pallet types** (no custom proxy types) and
**only Substrate software accounts** (no EVM-solo, no Ledger-EVM).

FR-148 is one functional requirement deliberately split into two stories by
capability. This story (US-17.1) is the **authority model** — the data and
management surface that says "account A is a Governance proxy for account B". The
sibling ([US-17.2](US-17.2-proxy-signing-sign-selector-and-proxied-by-display.md))
is the **signing-time behaviour** — the Sign Selector that, at the moment a
transaction is signed, picks which controlling account signs on behalf of the
proxied account and surfaces the proxied-by provenance. Splitting model from
signing keeps each story independently testable: this one can be verified by
authority CRUD against the pallet without exercising a live signing flow.

Materializes [FR-148](../../PRD.md#functional-requirements). **Retroactive** — already shipped.

## Acceptance criteria

- [x] **AC-1** — **Given** a Substrate account, **When** the user adds a proxy and
  picks a named proxy type (Any / NonTransfer / Governance / Staking / …),
  **Then** the delegated-authority relationship is established on-chain and shown
  in the wallet as "<proxy account> is a <type> proxy for <proxied account>".
- [x] **AC-2** — Only the named Polkadot proxy-pallet types are offered; no custom
  proxy type can be entered, and only Substrate software accounts are eligible
  (EVM-solo and Ledger-EVM accounts are not offered the proxy action) — per
  [AD-16](../../ARCHITECTURE.md#architecture-decisions).
- [x] **AC-3** — **Given** an existing proxy relationship, **When** the user views
  the account, **Then** all of its proxy relationships (as proxy and as proxied)
  are listed with their types, and a relationship can be removed.
- [x] **AC-4** — **Given** an attempt to add a proxy on an unsupported account
  type or with an unrecognised proxy type, **When** the user submits, **Then**
  the wallet rejects it with a clear error and establishes nothing on-chain.

## Tasks

- [x] **TASK-17.1.1** — Add-proxy flow: select proxy account + named pallet proxy type, build and submit the `proxy.addProxy` extrinsic (AC: 1, 2)
  - [x] Enumerate the supported named proxy types (Any, NonTransfer, Governance, Staking, …) from the pallet; no free-text custom type.
  - [x] Gate the proxy action to Substrate software accounts only (exclude EVM-solo / Ledger-EVM) per AD-16.
- [x] **TASK-17.1.2** — Proxy-relationship display & management: list an account's proxy relationships (as proxy / as proxied) with types, and support removal via `proxy.removeProxy` (AC: 1, 3)
- [x] **TASK-17.1.3** — Validation & error states for unsupported account type / unrecognised proxy type (AC: 4)

## Dev notes

### Architecture constraints

- [AD-16](../../ARCHITECTURE.md#architecture-decisions) — proxy is scoped to the
  *named* Polkadot proxy-pallet types (no custom types) and to Substrate
  accounts only; this story implements the type set and the eligibility gate.
- This story introduces no new AD entries; it materializes the model half of
  AD-16.

### Cross-story dependencies

- Required by [US-17.2](US-17.2-proxy-signing-sign-selector-and-proxied-by-display.md)
  — the Sign Selector enforces the authority/type relationship this story
  establishes; without the model there is no authority to check at signing time.
- Relates to the RequestService approval queue
  ([US-2.7](US-2.7-requestservice-approval-queue.md)) — the `addProxy` /
  `removeProxy` extrinsics are themselves signed through the existing sign queue.

### What we explicitly did NOT do

- No custom / arbitrary proxy types — only the named pallet types per AD-16.
  Trigger to revisit: a pallet upgrade that introduces a new named type.
- No EVM or hardware-wallet proxy accounts — Substrate software accounts only.
  Hardware-device signing is [EPIC-16](../epics/EPIC-16.md)'s transport.

### References

- [Source: PRD FR-148](../../PRD.md#functional-requirements) — proxy account management following Polkadot pallet proxy types
- [Source: ARCHITECTURE AD-16](../../ARCHITECTURE.md#architecture-decisions) — proxy account model
- [Issue #1676](https://github.com/Koniverse/SubWallet-Extension/issues/1676)
- [Issue #4725](https://github.com/Koniverse/SubWallet-Extension/issues/4725)

## Verification commands

| AC | Command |
|---|---|
| AC-1 | Manual: add a proxy with a named type to a Substrate account → relationship shown and on-chain |
| AC-2 | Manual: confirm only named types are offered and the proxy action is hidden for EVM-solo / Ledger-EVM accounts |
| AC-3 | Manual: view an account → all proxy relationships listed with types; remove one → relationship cleared |
| AC-4 | Manual: attempt an unsupported account / unrecognised type → rejected with error, nothing on-chain |

## Changelog entry

### Added
- Proxy account management for Substrate accounts: add a proxy with a named
  Polkadot proxy-pallet type (Any, NonTransfer, Governance, Staking, …), view
  and remove proxy relationships.

**Commit**:

## Implementation notes

Backfilled by US-21.2 (multi-agent trace + adversarial verify, run `wf_6b56f4cd-d08`; trace confidence: high, rule: first-delivery).

**Evidence:** CHANGELOG "## [1.3.72] — 2026-01-14": "Integrate Proxy Account Support (#4725)" — same issue #4725 the story cites; the 1.3.77 bullet "Improve Proxy account features (#4942)" only improves it, proving 1.3.72 is first delivery. Feature commits (Issue-4769/Issue-1676 branch, merged via PR #4789 on 2026-01-13) all pass git merge-base --is-ancestor against v1.3.72, and v1.3.72 is the earliest tag containing them.

Commits `da069575d07c1a13d7bd5597eaad5b2e931d9e16, 4be186b76c7b11c79a377611be4e10b1e62f97a5, 4c97547b8b31bb9ac584dffa746a9154f9343090` verified contained in the v1.3.72 anchor via `git merge-base --is-ancestor`; assignee resolved through the [US-21.1 contributor map](../../notes/contributor-map.md).

## Cross-references

- [PRD FR-148](../../PRD.md#functional-requirements)
- [Epic EPIC-17](../epics/EPIC-17.md)
- [US-17.2](US-17.2-proxy-signing-sign-selector-and-proxied-by-display.md)
- [ARCHITECTURE AD-16](../../ARCHITECTURE.md#architecture-decisions)
