---
id: US-11.6
title: "In-wallet swap via Asset Hub DEX"
epic: EPIC-11
status: done
priority: P1
points: 5
sprint:
version_shipped: 1.2.14
prd_ref: [FR-108]
arch_ref: [AD-24]
depends_on: [US-2.4]
assignee: frenkie-ng
commit: c11bb88efa, dfdf348d68
created: 2026-06-11
updated: 2026-06-11
---

## Goal

A user can swap Polkadot-ecosystem tokens on the Asset Hub DEX from inside the
wallet, with a live quote and single-confirm execution.

## Background

Asset Hub is Polkadot's system parachain for assets; its on-chain DEX
(`assetConversion`) lets users swap ecosystem tokens directly. It is integrated
as one **SwapService handler** (PRD [FR-8](../../PRD.md#functional-requirements), EPIC-2) — a new provider
is a new handler, never a new swap-UI branch — and quote/fee data is aggregated
via the Services SDK backend
([AD-24](../../ARCHITECTURE.md#architecture-decisions)). Execution is a Substrate
extrinsic on Asset Hub.

Materializes [FR-108](../../PRD.md#functional-requirements). **Retroactive** — already shipped.

## Acceptance criteria

- [x] **AC-1** — **Given** a pair tradable on the Asset Hub DEX, **When** the user
  enters an amount, **Then** a quote (rate, fee, slippage, min received) is shown.
- [x] **AC-2** — **Given** a quote, **When** the user confirms, **Then** the swap
  extrinsic is signed, submitted, and tracked in history.
- [x] **AC-3** — **Given** insufficient liquidity or a stale quote, **When** the
  user swaps, **Then** a clear error is shown and nothing is submitted.

## Tasks

- [x] **TASK-11.6.1** — Asset Hub DEX handler producing quotes via SwapService (AC: 1)
- [x] **TASK-11.6.2** — Build + sign + submit + track the swap extrinsic (AC: 2)
- [x] **TASK-11.6.3** — Liquidity / stale-quote error states (AC: 3)

## Dev notes

### Architecture constraints

- [AD-24](../../ARCHITECTURE.md#architecture-decisions) — quotes/fees via the Services SDK backend.
- Plugs into SwapService (FR-8); no Asset-Hub-specific swap-UI branch.

### Cross-story dependencies

- Builds on [US-2.4](US-2.4-swapservice-routing-engine.md) — the SwapService engine (FR-8) this handler registers into.

### Points justification

- 5 pts (§3a-bis) — one external-system integration (the Asset Hub on-chain DEX)
  behind the shared handler; a same-chain Substrate DEX comparable to the
  Hydration provider ([US-11.2](US-11.2-in-wallet-swap-via-hydration-dex.md)).

### References

- [Source: PRD FR-108](../../PRD.md#functional-requirements) — swap via Asset Hub DEX (Polkadot ecosystem)
- [Source: PRD FR-8](../../PRD.md#functional-requirements) — SwapService routing engine

## Verification commands

| AC | Command |
|---|---|
| AC-1 | Unit test: Asset Hub DEX handler returns a quote for a known pair |
| AC-2 | Manual: small swap on Asset Hub DEX → appears in history |
| AC-3 | Manual: no-liquidity pair → error, no submission |

## Changelog entry

### Added
- In-wallet swap via the Polkadot Asset Hub DEX (SwapService handler).

**Commit**:

## Implementation notes

Backfilled by US-21.2 (batch 1, commit `571f3085be`). Version `1.2.14` is the release whose docs/CHANGELOG.md bullet first delivers this story's headline capability; commits `c11bb88efa, dfdf348d68` were resolved from that bullet's issue number (`git log --grep`, filtered to a ±270-day window around the release date to exclude same-numbered upstream polkadot-js PRs) and each verified contained in the v1.2.14 anchor via `git merge-base --is-ancestor`. Assignee resolved through the [US-21.1 contributor map](../../notes/contributor-map.md).

## Cross-references

- [PRD FR-108](../../PRD.md#functional-requirements) · [Epic EPIC-11](../epics/EPIC-11.md) · [US-11.2](US-11.2-in-wallet-swap-via-hydration-dex.md)
