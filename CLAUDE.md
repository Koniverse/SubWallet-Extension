# CLAUDE.md

> This file is a thin pointer. **AGENTS.md is canonical.**
> On any conflict between AGENTS.md and CLAUDE.md, AGENTS.md wins.

See [AGENTS.md](AGENTS.md) for the full project guide.

## Koni-Docs Integration
koni-docs:
  plugins: []
  docs_path: docs/
  active_sprint: sprint-2026-W38
  version_file: VERSION

## Active Context <!-- koni-docs:auto-update -->
- Sprint: sprint-2026-W38 (2026-09-14 → 09-20) — **open**, **13 stories / 96 pts**: US-12.23, US-10.21 and US-5.17 (all **`done` — shipped in v1.3.90 on 09-17**), US-42.25 + US-42.26 (the 1.3.90 QC and release gate, both done 09-17), and eight tester-placed US-42.24 rows (1.3.78 / 1.3.79 / 1.3.80 / 1.3.84 done, **1.3.85 closed unrun** — 0 of 11 AC testable on Mobile, parent + regression + verify in progress; regression re-pointed 13 → 20). Table updated each day rows landed — first window told in time. W37 **closed 2026-09-14 at 8 of 12 stories, 77 of 103 pts** — every done story was tester QC, the one dev story carried.
- **Two windows in a row under-reported at closeout, and by more the second time.** W36's table said 2/13 while 10/63 were worked; W37's said 1/5 while 12/103 were — the tester put eleven US-42.24 stories on `sprint: sprint-2026-W37` through their own PRs (#5077–#5080) and nothing re-read the field. **Re-read every story's `sprint:` at each sync; the scope table is not the source.** Rows the tester places are listed, never moved.
- **The web-runner QC programme**: extended to 1.3.90 on 09-17 — **24 sub-tasks**: 16 done (132 pts), 1 closed unrun (8), 3 in progress on W38 (28), **4 new in `backlog` for 1.3.87–1.3.90 (29 pts)**. **The retest landed 09-16** (#5083): US-42.24.20 reads 54 fixed · 2 no-fix · 15 not fixed yet; **11 of the 15 are items the developer had already ticked on #2057**, one a P1 crash (`BUG-42.24.19-16`, item 59). Tester-owned; **do not move these stories between sprints**.
- **US-42.24.19 (regression) is where the bugs are** — 7 bugs (`BUG-42.24.19-01…-07`) against app-wide behaviour, not any single version: first-run freeze, NFT transfer refusal, custom-network crash. US-42.24.20 is the retest story that rechecks each on a fixed build.
- **#2057 Mobile web-runner tracker** lives in [docs/notes/2026-09-04.md](docs/notes/2026-09-04.md) — tick-list, dev-side fix status only; the tester's verification is US-42.24.20 and is not mirrored. **The tracker spans several comments — three as of 09-14; read every one.** Retitled to **1.3.90** on 09-17 with four version blocks added; 1.3.87 / 1.3.88 / 1.3.89 ticked by 09-18, **only the 1.3.90 pair open — it needs Mobile screens, not just the bundle**. Item 63 (WalletConnect on EVM, **two P1s** `BUG-42.24.19-17`/`-18`) ticked whole 09-18; item 9 is the only P1 still open.
- **v1.3.90 shipped 2026-09-17 09:06 UTC** (`1f9b2a4cb6`, from `master`): #5072 VRF signing + #5064 Bittensor manual claim, **plus the unlisted passkey-unlock offer** (`89e3c05e17`, rode in #5076, no issue → [US-5.17](docs/sprints/stories/US-5.17-passkey-unlock-setup-offer.md)). Both PRs merged that morning by saltict — #5065 after **four windows** with a fresh approval on the final head, #5076 on the fixed head `67939c4315`. Lineage verified per SHA. **The release gate closed the same day** — US-42.25 (VRF, 11/11 on the PR build) and US-42.26 (dev → master → draft → production, **12/14**), both opened and closed 09-17 by the tester; 1.3.89's gate had lagged ten days. **The two unticked gate ACs are the two-origin property VRF exists for** (AC-13/14, *skipped* — the test dApp is reachable from one origin only); it shipped verified by unit test, never by hand.
- **All three shipped without a unit test** — US-12.23's AC-3/4 (fixed-point decode, threshold fallback), the AC-10 test the reviewer asked for on US-10.21, and US-5.17's gate; US-5.17's enrollment-failure path is also the one build check no QC story ran. **US-12.23's AC-4 was wrong at ship time**: the story said the threshold fallback was `500000` rao; `0071a73f79` made it `0` — *filter nothing, let the chain decide* — which is the fee-charging-no-op case AC-5's guard exists for and cannot see. Corrected in the story with the correction visible. Four of five locales carry the English threshold message.
- **8 W33 stories still not carried** — US-4.21/4.22/4.23, US-5.10, US-10.11, US-12.11, US-13.11, US-20.4 (32 pts). Re-checked 2026-08-25: all 6 anchors OPEN with last-touched dates **byte-identical to the 08-18 check** (#4451 2025-11-18 · #4889/#4424 2026-01-19 · #4946 2026-03-24 · #4984 2026-04-08 · #4995 2026-05-21). They have now sat out **three windows** (W31, W33, W34). Open planning call, a month old.
- **The board cannot be re-read** — `projectV2` needs `read:project`; the available token has `gist, read:org, repo`. Resync from `gh issue view` / `gh pr view` / git, and claim no board column as current.
- **12 W31 stories are live work in a closed window** — they keep `sprint: sprint-2026-W31` with status `ready`/`in-progress`/`review`. `validate` passes, so nothing flags it. Open planning call: US-1.4, US-1.5, US-4.14, US-4.15, US-4.19, US-4.20, US-8.12, US-10.9, US-15.4, US-16.3, US-19.9, US-20.2.
- Umbrella issues belong to their **epic**, not a story (rule 10) — grep `epics/` as well as `stories/` before calling an issue uncovered.
- Board ↔ docs: the Projects board (#2) numbers its `Week` iterations **one behind ISO**; docs sprint IDs are ISO. Dates align 1:1 — board `Week 32 - 2026` **is** `sprint-2026-W33`, so `Week 34 - 2026` is `sprint-2026-W35`.
- Last Version: **1.3.90** (released 2026-09-17 from `master`, `1f9b2a4cb6` — #5072 VRF signing + #5064 manual claim + unlisted US-5.17). Lineage verified per SHA: all seven feature/merge commits are ancestors and `git tag --contains` returns exactly `v1.3.90`. 1.3.89 was `b9363157d0`, 2026-08-28.
- **The root `CHANGELOG.md` is behind `master`** — no 1.3.89 or 1.3.90 section, because what reaches `dev` comes from `subwallet-dev`, not `master`. Left alone on purpose: that file is the untouched legacy copy and it will arrive when `master` does. Not a miss. **1.3.90's release note lists two items; three shipped** — the passkey offer is only in `docs/CHANGELOG.md`.
- **LESSONS §69 has now appeared in three shapes in one month** — the closing-PR field, then the issue *state*, then an **empty search result**. On 2026-08-25 I claimed #5062's chainlist half had "no issue and no PR"; [ChainList PR #709](https://github.com/Koniverse/SubWallet-ChainList/pull/709) had merged 08-24. The query looked for issues, not PRs, in a repo where the PR is titled with the *Extension* number. **A negative finding needs the same corroboration as a positive one**, and cross-repo work needs both repos searched for PRs as well as issues.
- **Three empty-body issues in under a month** — #5058, #5064, now **#5072** (sr25519 VRF signing for dApp key derivation, 2026-09-05) → [US-10.21](docs/sprints/stories/US-10.21-sr25519-vrf-signing-dapp-key-derivation.md), now `done` in 1.3.90, written from PR #5076's diff, EPIC-10 placement **provisional**. The first two were explained by their own PR, never by the issue; neither ever gained a body. Write stories from the diff, say so, and do not invent ACs.
- **PR #5053 merged without a re-review of its final head** — both approvals dated 2026-08-10, five of eight commits landed after. Flagged before the merge (2026-08-18), never closed; it shipped and QC passed 10/10. Process point, recorded not dropped.
- **`check-ids` exits 1** on US-42.10 — `version_shipped: 1.2.44(532)b-v16` is a SubWallet-**Mobile** build, not a release of this product (rule 1b), and EPIC-42 is `prd_ref: []` so the field belongs empty (rule 4). Sibling pages US-42.6 / US-42.9 leave it blank. One-field fix; **applied 2026-08-13, reverted, still outstanding.**
- Recent Decisions: D105 (fork boundary is its own window) · D106 (`commit:` names what made the capability true) · D107 (a ticked AC is a claim about the code) · D108 (every tracker issue gets a story in a 20-epic maintenance layer; FR map stays clean) · D109 (the epic matrix's requirement column has five meanings — legend written once, epics link to it)
- Recent Lessons: §69 (a link an API hands you is a claim, not a fact — GitHub's closing-PR field was 41% wrong; verify against the developer's [Issue-N] title) · §70 (unrecoverable is a claim too — 367 blank versions sat one `git tag --contains` away; check git before declaring a gap) · §71 (import runs the unified-account migration inline at per-keypair cost; interrupting it leaves accounts that cannot sign)

See `.active-context.md` (gitignored, per-developer) for live snapshot;
copy from `.active-context.example.md` on first checkout.
