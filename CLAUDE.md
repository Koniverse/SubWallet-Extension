# CLAUDE.md

> This file is a thin pointer. **AGENTS.md is canonical.**
> On any conflict between AGENTS.md and CLAUDE.md, AGENTS.md wins.

See [AGENTS.md](AGENTS.md) for the full project guide.

## Koni-Docs Integration
koni-docs:
  plugins: []
  docs_path: docs/
  active_sprint: sprint-2026-W39
  version_file: VERSION

## Active Context <!-- koni-docs:auto-update -->
- Sprint: sprint-2026-W39 (2026-09-21 → 09-27) — opened 2026-09-24 **mid-window** at 0 dev points; **it did not stay empty**. 09-25: [PR #5089](https://github.com/Koniverse/SubWallet-Extension/pull/5089) *Support ERC-20 ERC-721 for X Layer*, by tunghp2002 onto `subwallet-dev` — **one line**, and **#5088 is the same work, not a second piece**: opened 04:40 against `master`, closed 04:49, replaced by #5089 on the right base with the same branch and the same commit; `ChainListVersion` `0.2.129` → `0.2.131` in `chain-service/utils/patch.ts`; open, no body, no review. The capability is in **ChainList #711**, not here, so **no story: EPIC-4 owns it** (rule 10, same footing as #4935) and it becomes a US-4.3 row only if it ships a CHANGELOG line. W38 **closed 2026-09-24 at 12 of 17 stories, 76 of 130 pts**.
- **Three windows in a row under-reported at closeout.** W36: table said 2/13, 10/63 worked. W37: 1/5 vs 12/103. W38: 13/96 vs **17/130** — the tester moved the four 1.3.87–1.3.90 sub-tasks onto W38 on 09-18 (PR #5086) and nothing re-read the field for six days. **Re-read every story's `sprint:` at each sync; the scope table is not the source.** The rule was already written when W38 broke it — it fails when nobody syncs, not when nobody knows. Rows the tester places are listed, never moved.
- **The web-runner QC programme**: **23 sub-tasks** (numbered .1–.24, **.3 absent**) plus the parent — **20 done (166 pts), 1 closed unrun (8), 2 in progress (28)**; nothing in `backlog`, **nothing `blocked`**. **`BUG-42.24.24-01` is resolved**: unclaimed rewards read 0 TAO on Mobile from 09-18, verified fixed **09-24**, US-42.24.24 closed **`done` 21 of 22**. It was open six days and **US-12.23 AC-3 had already named the failure mode** (`I96F32` decode, *a plausible wrong number*) with no test behind it or behind AC-4's `0` fallback; the record never says which it was, or which side fixed it. **`blocked` now has no user at all** — 0 stories in 336, and still no enum check in `validate`. **No tick is contradicted by a retest any more**: US-42.24.20 reads **72 fixed · 5 no-fix · 4 not fixed yet** (81 rows), against 54 · 2 · 15 (71) on 09-16 — the eleven rows the developer had ticked and the tester could not verify **all passed**, including a P1 crash. Of the 4 still open, 2 were never ticked (`-19-03`, `-19-06`); the other 2, **`-19-23` and `-19-24`, were both ticked 09-26 after the 09-25 retest ran** — lag, the item-58 shape, and both need one rerun to close. Tester-owned; **do not move these stories between sprints**.
- **US-42.24.19 (regression) is where the bugs are** — 7 bugs (`BUG-42.24.19-01…-07`) against app-wide behaviour, not any single version: first-run freeze, NFT transfer refusal, custom-network crash. US-42.24.20 is the retest story that rechecks each on a fixed build.
- **#2057 Mobile web-runner tracker** lives in [docs/notes/2026-09-04.md](docs/notes/2026-09-04.md) — dev-side tick status only; the tester's verification is US-42.24.20 and is not mirrored. **The tracker spans four comments as of 09-25; read every one.** **66 of 71 done — the cleanest this tracker has been**: every item filed after 09-03 is ticked. Open: 6 and 9 (the two originals, never ticked), 35 (**verified fixed, simply unticked**), 42 and 69 (deferred with a stated reason). Items 70 and 71 were filed 09-25 as the first to arrive **with their bug rows already logged**, and both were ticked 09-26 (04:42, 07:18) — **a one-day turnaround on both**. 71 was the third finding in a row on the claim-rewards screen after 68 and 69; **all three are now settled on the developer's side**. Regression round 2 is running: checklist 77 → 82 lines, **Android 37/82, iOS 21/82**.
- **v1.3.90 shipped 2026-09-17 09:06 UTC** (`1f9b2a4cb6`, from `master`): #5072 VRF signing + #5064 Bittensor manual claim, **plus the unlisted passkey-unlock offer** (`89e3c05e17`, rode in #5076, no issue → [US-5.17](docs/sprints/stories/US-5.17-passkey-unlock-setup-offer.md)). Both PRs merged that morning by saltict — #5065 after **four windows** with a fresh approval on the final head, #5076 on the fixed head `67939c4315`. Lineage verified per SHA. **The release gate closed the same day** — US-42.25 (VRF, 11/11 on the PR build) and US-42.26 (dev → master → draft → production, **12/14**), both opened and closed 09-17 by the tester; 1.3.89's gate had lagged ten days. **The two unticked gate ACs are the two-origin property VRF exists for** (AC-13/14, *skipped* — the test dApp is reachable from one origin only); it shipped verified by unit test, never by hand.
- **All three shipped without a unit test** — US-12.23's AC-3/4 (fixed-point decode, threshold fallback), the AC-10 test the reviewer asked for on US-10.21, and US-5.17's gate; US-5.17's enrollment-failure path is also the one build check no QC story ran. **US-12.23's AC-4 was wrong at ship time**: the story said the threshold fallback was `500000` rao; `0071a73f79` made it `0` — *filter nothing, let the chain decide* — which is the fee-charging-no-op case AC-5's guard exists for and cannot see. Corrected in the story with the correction visible. Four of five locales carry the English threshold message.
- **20 stories carry a live status in a window that closed months ago** — 12 on W31 (64 pts) and 8 on W33 (32 pts), **96 points**, statuses `ready` / `in-progress` / `review`. W33's six anchors have now been read **three times — 08-18, 08-25, 09-26 — byte-identical every time**, so this is no longer *stalled*: **no work is in flight on any of them** (#4451 silent 10 months; the two with open PRs, #5001 and #4996, are themselves from June and May). `validate` passes, nothing checks that `sprint:` names an open window, and these are the same rows that made W36/W37/W38 under-report at closeout. Evidence and the recommended end state — `status: backlog`, `sprint:` cleared — in [docs/notes/2026-09-26.md](docs/notes/2026-09-26.md). **Open planning call, raised in four windows running; not actioned because twenty frontmatter edits are a board decision.**
- **The board cannot be re-read** — `projectV2` needs `read:project`; the available token has `gist, read:org, repo`. Resync from `gh issue view` / `gh pr view` / git, and claim no board column as current.
- Umbrella issues belong to their **epic**, not a story (rule 10) — grep `epics/` as well as `stories/` before calling an issue uncovered.
- Board ↔ docs: the Projects board (#2) numbers its `Week` iterations **one behind ISO**; docs sprint IDs are ISO. Dates align 1:1 — board `Week 32 - 2026` **is** `sprint-2026-W33`, so `Week 34 - 2026` is `sprint-2026-W35`.
- Last Version: **1.3.90** (released 2026-09-17 from `master`, `1f9b2a4cb6` — #5072 VRF signing + #5064 manual claim + unlisted US-5.17). Lineage verified per SHA: all seven feature/merge commits are ancestors and `git tag --contains` returns exactly `v1.3.90`. 1.3.89 was `b9363157d0`, 2026-08-28.
- **The root `CHANGELOG.md` is behind `master`** — no 1.3.89 or 1.3.90 section, because what reaches `dev` comes from `subwallet-dev`, not `master`. Left alone on purpose: that file is the untouched legacy copy and it will arrive when `master` does. Not a miss. **1.3.90's release note lists two items; three shipped** — the passkey offer is only in `docs/CHANGELOG.md`.
- **LESSONS §69 has now appeared in four shapes** — the closing-PR field, the issue *state*, an **empty search result** (#5062's chainlist half: [ChainList PR #709](https://github.com/Koniverse/SubWallet-ChainList/pull/709) had merged; the query looked for issues, not PRs, in a repo where the PR is titled with the *Extension* number), and now **the mirror image** — ChainList #711's work arrives as an *Extension* PR (#5089) titled from the *ChainList* issue. **A negative finding needs the same corroboration as a positive one**, and cross-repo work needs both repos searched for PRs as well as issues, **in both naming directions**.
- **Four empty-body issues in six weeks** — #5058, #5064, #5072, now **ChainList #711** (*Support ERC-20 ERC-721 for X Layer*, 2026-09-25). None ever gained a body; each was explained only by its PR. Write stories from the diff, say so, and do not invent ACs.
- **PR #5053 merged without a re-review of its final head** — both approvals dated 2026-08-10, five of eight commits landed after. Flagged before the merge (2026-08-18), never closed; it shipped and QC passed 10/10. Process point, recorded not dropped.
- **`check-ids` exits 1** on US-42.10 — `version_shipped: 1.2.44(532)b-v16` is a SubWallet-**Mobile** build, not a release of this product (rule 1b), and EPIC-42 is `prd_ref: []` so the field belongs empty (rule 4). Sibling pages US-42.6 / US-42.9 leave it blank. One-field fix; **applied 2026-08-13, reverted, still outstanding.**
- Recent Decisions: D105 (fork boundary is its own window) · D106 (`commit:` names what made the capability true) · D107 (a ticked AC is a claim about the code) · D108 (every tracker issue gets a story in a 20-epic maintenance layer; FR map stays clean) · D109 (the epic matrix's requirement column has five meanings — legend written once, epics link to it)
- Recent Lessons: §69 (a link an API hands you is a claim, not a fact — GitHub's closing-PR field was 41% wrong; verify against the developer's [Issue-N] title) · §70 (unrecoverable is a claim too — 367 blank versions sat one `git tag --contains` away; check git before declaring a gap) · §71 (import runs the unified-account migration inline at per-keypair cost; interrupting it leaves accounts that cannot sign)

See `.active-context.md` (gitignored, per-developer) for live snapshot;
copy from `.active-context.example.md` on first checkout.
